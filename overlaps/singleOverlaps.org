:PROPERTIES:
:GPTEL_MODEL: claude-opus-4-7
:GPTEL_BACKEND: Anthropic
:GPTEL_SYSTEM: You are a large language model embedded in Emacs, serving as a research\nassistant to an expert in machine learning and computational\nneuroscience. You support the full research loop: reading and\nsynthesizing literature, developing ideas, deriving math, designing\nexperiments, implementing and debugging code, analyzing data, and\npreparing results for communication.\n\n# Core principles\n\n- Be concise and precise; avoid redundancy. Prefer information density\n  over hedging and restatement.\n- Think step-by-step before answering. Share the reasoning the user\n  needs to verify the result; hide the rest.\n- State assumptions explicitly. If a question is ambiguous or\n  underspecified, ask one focused clarifying question rather than\n  guessing.\n- Calibrate confidence. Distinguish established results from plausible\n  conjecture; flag uncertainty and cite sources (papers, docs, RFCs)\n  for non-obvious claims.\n- Never fabricate. No invented citations, numbers, APIs, datasets, or\n  experimental results. Use `[CITE: topic]` / `[CHECK: claim]` /\n  `[TODO: ...]` placeholders when information is missing.\n\n# Research workflow support\n\n## Literature and synthesis\n- When summarizing a paper: (1) problem and gap, (2) key idea in one\n  sentence, (3) method sketch, (4) main results with one quantitative\n  anchor, (5) limitations, (6) how it relates to the user's context.\n- When comparing papers: organize by dimension (assumption, method,\n  data, metric, result), not by paper. Use a table when ≥3 items.\n- Distinguish what a paper *shows* from what it *claims*.\n\n## Ideation and critique\n- When brainstorming: generate 3–5 distinct options spanning the\n  design space, not variants of one. Note trade-offs.\n- When critiquing an idea: give the strongest version of the idea\n  first, then failure modes, then concrete tests that would\n  discriminate.\n- Prefer falsifiable predictions over vague intuitions.\n\n## Math and derivations\n- Define all symbols on first use; keep notation consistent across a\n  session. Specify tensor shapes and domains.\n- Show derivation steps with a short justification per step. Mark\n  approximations (`≈`), limits, and regimes of validity.\n- Sanity-check results: units, limiting cases, dimensions, symmetry,\n  sign.\n- Use `\[ ... \]` / `$...$` in Org; `align` inside `#+begin_src latex`\n  for multi-line.\n\n## Experiment design and analysis\n- Before running: state hypothesis, independent/dependent variables,\n  controls, expected effect size, required n, and the analysis plan.\n- Prefer pre-registered analyses; mark post-hoc analyses as such.\n- Report effect sizes and uncertainty (CIs, SEM, bootstrap) with\n  sample size, not just p-values. Name the statistical test and its\n  assumptions; note multiple-comparison corrections.\n- In ML: specify splits, seeds, baselines matched for compute/data,\n  and metric definitions. Distinguish tuning from test performance.\n- In neuroscience: state the level of abstraction (biophysical, rate,\n  spiking, normative), the data modality, and the link between model\n  and measurement. Distinguish "fits" from "predicts held-out".\n\n# Code and computation\n\n- Write clean, idiomatic, minimal code. Prefer standard libraries and\n  well-maintained packages; note version assumptions.\n- Comment the *why*, not the *what*. Include type hints in Python and\n  docstrings for non-trivial functions.\n- Make examples self-contained and runnable; show expected output\n  when useful.\n- For numerical/ML code: seed RNGs, annotate tensor shapes, prefer\n  vectorized ops, and guard against silent broadcasting bugs.\n- For data analysis: show the pipeline (load → clean → transform →\n  model → evaluate → visualize) and state what each step assumes\n  about the data.\n- When debugging: ask for the exact error, the minimal reproducer, and\n  the environment before proposing fixes.\n\n# Output format (Emacs-native)\n\n- Default to Org-mode: headings (`*`, `**`), source blocks\n  (`#+begin_src ... #+end_src`), LaTeX math, and tables.\n- Use `jupyter-python` blocks for runnable Python; `emacs-lisp` for\n  Emacs configuration; `sh`/`bash` for shell (mark destructive\n  commands).\n- Use Org-cite (`[cite:@key]`) for references when available.\n- For file or code edits, return a minimal diff or the specific edited\n  region rather than the full file, unless asked.\n\n# Interaction modes (state which you are in when relevant)\n\n1. **Explain**: teach a concept at the requested level; give a worked\n   example and a pointer to a canonical reference.\n2. **Summarize**: condense a paper, thread, or notes using the\n   structure above.\n3. **Brainstorm**: generate diverse options with trade-offs.\n4. **Critique**: steelman then stress-test; propose discriminating\n   experiments.\n5. **Derive**: produce step-by-step math with sanity checks.\n6. **Implement**: write code to spec, with assumptions listed.\n7. **Debug**: diagnose from a minimal reproducer; propose the smallest\n   fix first.\n8. **Analyze**: run/describe a statistical or ML analysis with\n   explicit assumptions and uncertainty.\n\n# Guardrails\n\n- Do not invent citations, authors, years, DOIs, dataset statistics,\n  benchmark numbers, or API signatures.\n- Do not soften honest limitations or known failure modes.\n- If asked to produce claims stronger than evidence supports, push\n  back once and then comply, marking the passage `[OVERCLAIM?]`.\n- If a task requires external information you don't have (paper\n  contents, dataset details, library version), say so and ask.\n
:GPTEL_TOOLS:
:END:
#+STARTUP: fold
#+PROPERTY: header-args:jupyter-python :results both :exports both :async yes :session overlaps :kernel dual :output-dir ./figures/overlaps :file (lc/org-babel-tangle-figure-filename)

* Imports

#+begin_src jupyter-python
  from sklearn.exceptions import ConvergenceWarning
  import warnings
  warnings.filterwarnings("ignore")
  import sys

  import os
  if not sys.warnoptions:
    warnings.simplefilter("ignore")
    os.environ["PYTHONWARNINGS"] = "ignore"

  import pickle as pkl
  import numpy as np
  import matplotlib.pyplot as plt
  import pandas as pd
  import seaborn as sns

  from sklearn.base import clone
  from sklearn.preprocessing import StandardScaler
  from sklearn.model_selection import RepeatedStratifiedKFold, StratifiedKFold

  from src.common.plot_utils import add_vlines, add_vdashed
  from src.common.options import set_options
  from src.common.get_data import get_X_y_days
#+end_src

#+RESULTS:

* Notebook Settings

#+begin_src jupyter-python
%load_ext autoreload
%autoreload 2
%reload_ext autoreload

%run /home/leon/dual/notebooks/setup.py
%config InlineBackend.figure_format = 'png'
#+end_src

#+RESULTS:
: The autoreload extension is already loaded. To reload it, use:
:   %reload_ext autoreload
: Python exe
: /home/leon/mambaforge/envs/dual/bin/python

* Helpers
** pad

#+begin_src jupyter-python
def pad_list(arrays, axis=0, max_len=None):
    """
    Pads a list of arrays along the specified axis with NaNs so all have the same size along that axis.
    Returns a list of padded arrays.
    """
    # Find maximum size along specified axis
    if max_len is None:
        max_len = max(arr.shape[axis] for arr in arrays)

    padded = []
    for arr in arrays:
        pad_width = [(0, 0)] * arr.ndim


        n_pad = max_len - arr.shape[axis]

        if n_pad > 0:
            pad_width[axis] = (0, n_pad)
            arr_padded = np.pad(arr, pad_width, mode='constant', constant_values=np.nan)
        else:
            arr_padded = arr
        padded.append(arr_padded)

    return padded
#+end_src

#+RESULTS:

#+begin_src jupyter-python
def pad_with_nans(array, target_shape):
    result = np.full(target_shape, np.nan)  # Create an array filled with NaNs
    print(result.shape)
    slices = tuple(slice(0, min(dim, target)) for dim, target in zip(array.shape, target_shape))
    result[slices] = array[slices]
    return result
#+end_src

#+RESULTS:

** save

#+begin_src jupyter-python
  def convert_seconds(seconds):
      h = seconds // 3600
      m = (seconds % 3600) // 60
      s = seconds % 60
      return h, m, s
#+end_src

#+RESULTS:

#+begin_src jupyter-python
  import pickle as pkl

  def pkl_save(obj, name, path="."):
      os.makedirs(path, exist_ok=True)
      destination = path + "/" + name + ".pkl"
      print("saving to", destination)
      pkl.dump(obj, open(destination, "wb"))


  def pkl_load(name, path="."):
      source = path + "/" + name + '.pkl'
      print('loading from', source)
      return pkl.load(open( source, "rb"))

#+end_src

#+RESULTS:

* Parameters

#+begin_src jupyter-python
SCALE=None
#+end_src

#+RESULTS:

#+begin_src jupyter-python
old_mice = ['ChRM04','JawsM15', 'JawsM18', 'ACCM03', 'ACCM04']
Jaws_mice = ['JawsM01', 'JawsM06', 'JawsM12', 'JawsM15', 'JawsM18']

mice = ['JawsM01', 'JawsM06', 'JawsM12', 'JawsM15', 'JawsM18', 'ChRM04', 'ChRM23', 'ACCM03', 'ACCM04']
laser_mice = ['JawsM01', 'JawsM06', 'JawsM12', 'JawsM15', 'JawsM18', 'ChRM04', 'ChRM23']
# mice = ['JawsM01', 'JawsM06', 'JawsM12', 'JawsM15', 'JawsM18', 'ChRM04', 'ChRM23']
# mice = ['JawsM15', 'JawsM18', 'ChRM04']

tasks = ['Dual'] # all

kwargs = {
   'mice': mice,
   'tasks': tasks,
   'mouse': mice[0], 'laser': 0,
   'trials': '', 'reload': 0, 'data_type': 'dF',
   'prescreen': None, 'pval': 0.05,
   'preprocess': None, 'scaler_BL': 'standard_BL',
   'avg_noise': False, 'unit_var_BL': False,
   'random_state': None, 'T_WINDOW': 0.0,
   'l1_ratio': 0.95,
   'n_comp': 3, 'pca': 'pca',
   'scaler': None,
   'bootstrap': 1, 'n_boots': 128,
   'n_splits': 5, 'n_repeats': 10,
   'class_weight': 0,
   'multilabel': 0,
   'mne_estimator':'generalizing', # sliding or generalizing
   'n_jobs': 64,
}

kwargs['old_mice'] = ['ChRM04','JawsM15', 'JawsM18', 'ACCM03', 'ACCM04']
kwargs['new_mice'] = ['JawsM01', 'JawsM06', 'JawsM12', 'ChRM23']
kwargs['Jaws_mice'] = ['JawsM01', 'JawsM06', 'JawsM12', 'JawsM15', 'JawsM18']

# kwargs['days'] = ['first', 'middle', 'last']
kwargs['days'] = ['first', 'last']
# kwargs['days'] = 'all'
options = set_options(**kwargs)
#+end_src

#+RESULTS:

* Loading Raw Data

#+begin_src jupyter-python
X_trials, y_trials = [], []

n_neurons = 3319
counter = 0
options['reload'] = 0
for mouse in options['mice']:

    options['mouse'] = mouse
    options = set_options(**options)
    X, y = get_X_y_days(**options)

    print(mouse, X.shape, y.shape, options['n_days'])

    X_scale = X.copy()
    std_=1
    for day in range(1, options['n_days']+1):
        idx0 = (y.day==day) & (y.laser==0)

        mean_ = 1
        # if SCALE is not None:
        #     mean_ = np.nanmean(X[idx0], 0, keepdims=1)

        # if SCALE=='std':
        #     std_ = np.nanstd(X[idx0], (0, 2), ddof=0, keepdims=1)
        #     lo, hi = np.percentile(std_.ravel(), [5, 95])
        #     std_ = np.clip(std_, lo, hi)

        idx = (y.day==day)
        X_scale[idx] = (X[idx] - mean_) / std_

    X_trial = np.zeros((X.shape[0], n_neurons, X.shape[-1])) * np.nan
    X_trial[:, counter:X.shape[1]+counter, :] = X_scale

    counter += X.shape[1]

    X_trials.append(X_trial)

    y['mouse'] = mouse
    y_trials.append(y)

X_trials = np.array(X_trials, dtype=object)
y_trials = np.array(y_trials, dtype=object)
#+end_src


#+begin_src jupyter-python
X_all = np.concatenate(X_trials, 0)
y_all = pd.concat(y_trials)
print(X_all.shape, y_all.shape)
#+end_src

#+RESULTS:
: (9216, 3319, 84) (9216, 16)

#+begin_src jupyter-python
dum = ''
if SCALE is not None:
    dum = SCALE

pkl_save(X_all, 'X_all_nan_%s' % dum, path="../data/pca")
pkl_save(y_all, 'y_all_nan_%s' % dum, path="../data/pca")
#+end_src

#+RESULTS:
: saving to ../data/pca/X_all_nan_.pkl
: saving to ../data/pca/y_all_nan_.pkl

* CCGD Analysis
** Loading Calcium data

#+begin_src jupyter-python
dum = ''
if SCALE is not None:
    dum = SCALE

X_all = pkl_load('X_all_nan_%s' % dum, path="../data/pca")
y_all = pkl_load('y_all_nan_%s' % dum, path="../data/pca")
#+end_src

#+RESULTS:
: loading from ../data/pca/X_all_nan_.pkl
: loading from ../data/pca/y_all_nan_.pkl

#+begin_src jupyter-python
y_all['sample'] = y_all.sample_odor
y_all['distractor'] = y_all.dist_odor
y_all['test'] = y_all.test_odor

y_all['licks'] = (y_all.odr_choice + y_all.choice)
y_all['licks'] = y_all['licks']>0
#+end_src

#+RESULTS:

#+begin_src jupyter-python
print(y_all.keys())
#+end_src

#+RESULTS:
: Index(['sample_odor', 'dist_odor', 'test_odor', 'tasks', 'response', 'laser',
:        'day', 'choice', 'pair', 'odr_perf', 'odr_choice', 'odr_response',
:        'odor_pair', 'learning', 'performance', 'mouse', 'sample', 'distractor',
:        'test', 'licks'],
:       dtype='object')

** Cross condition generalized decoding
*** utils
**** Imports

#+begin_src jupyter-python
import mne
import numpy as np
import pandas as pd
from scipy.ndimage import gaussian_filter1d
from sklearn.base import clone
from sklearn.pipeline import Pipeline
from sklearn.preprocessing import StandardScaler
from sklearn.model_selection import StratifiedKFold
from sklearn.linear_model import LogisticRegressionCV, LogisticRegression
from sklearn.feature_selection import SelectPercentile, SelectKBest, SelectFdr, SelectFpr, f_classif
from mne.decoding import SlidingEstimator, GeneralizingEstimator
from joblib import Parallel, delayed

mne.set_log_level("ERROR")
#+end_src

#+RESULTS:

**** Preprocessing

#+begin_src jupyter-python
def smooth_and_bin2(X, sigma=1.0):
    """Gaussian-smooth along time and bin by pairs of samples."""
    X_s = gaussian_filter1d(X, sigma=sigma, axis=-1, mode="nearest")
    T = X_s.shape[-1]
    assert T % 2 == 0
    return X_s.reshape(*X_s.shape[:-1], T // 2, 2).mean(-1)
#+end_src

#+RESULTS:

**** Estimator factory

#+begin_src jupyter-python
def get_estimator(
    clf="logcv",
    scoring="accuracy",
    mode="generalizing",
    scaler=None,
    l1_ratio=0,
    C=0.1,
    Cs=np.logspace(-4, 4, 10),
    n_jobs=-1,
):
    solver = "liblinear" if l1_ratio in (0, 1) else "saga"

    if clf == "logcv":
        clf = LogisticRegressionCV(
            cv=5, solver=solver, class_weight="balanced",
            n_jobs=None, l1_ratios=(l1_ratio,), Cs=Cs,
        )
    else:
        clf = LogisticRegression(
            solver=solver, class_weight="balanced",
            n_jobs=None, l1_ratio=l1_ratio,
        )

    pipeline = []
    if scaler == "standard":
        pipeline.append(("scaler", StandardScaler()))
    elif scaler == "center":
        pipeline.append(("scaler", StandardScaler(with_std=False)))

    # pipeline.append("selector", SelectFpr(f_classif, alpha=0.05))
    pipeline.append(("model", clf))
    pipe = Pipeline(pipeline)

    if mode == "sliding":
        return SlidingEstimator(pipe, scoring=scoring, n_jobs=n_jobs, verbose=False)
    if mode == "generalizing":
        return GeneralizingEstimator(pipe, scoring=scoring, n_jobs=n_jobs, verbose=False)
    return pipe
#+end_src

#+RESULTS:

**** Weight / projection utilities

#+begin_src jupyter-python
def get_space_params_timewise(est, raw=False, min_scale=1e-8):
    """
    Extract per-timepoint linear weights and intercepts from a fitted
    SlidingEstimator / GeneralizingEstimator.

    Parameters
    ----------
    raw : bool
        If True, back-project weights through the scaler:
            w_raw = w_scaled / scale
            b_raw = b_scaled - w_raw . mean
    min_scale : float
        Lower bound on scaler.scale_ to avoid blow-up from near-zero-variance
        features.

    Returns
    -------
    ws : (n_train_times, n_features)
    bs : (n_train_times,)
    means : (n_train_times, n_features) or None
        Scaler means per train-time; needed to undo intercept drop in raw space.
    """
    ws, bs, means = [], [], []
    any_mean = False

    for est_t in est.estimators_:
        if isinstance(est_t, Pipeline):
            clf = est_t.named_steps["model"]
            scaler = est_t.named_steps.get("scaler", None)
        else:
            clf, scaler = est_t, None

        w = np.asarray(clf.coef_).ravel().astype(float)
        b = float(np.asarray(clf.intercept_).item())

        mu = None
        if scaler is not None:
            mean = getattr(scaler, "mean_", None)
            scale = getattr(scaler, "scale_", None)
            mean = np.zeros_like(w) if mean is None else np.asarray(mean)
            scale = np.ones_like(w) if scale is None else np.asarray(scale)
            scale = np.where(scale < min_scale, 1.0, scale)

            if raw:
                w = w / scale
                b = b - np.dot(w, mean)
                mu = mean
                any_mean = True

        ws.append(w)
        bs.append(b)
        means.append(mu)

    ws = np.asarray(ws)
    bs = np.asarray(bs)
    means = np.asarray(means) if any_mean else None
    return ws, bs, means


def _apply_scaler_if_needed(est, X, raw):
    """If raw=False and pipeline has a scaler, transform X per-time."""
    if raw:
        return X
    first = est.estimators_[0]
    if not isinstance(first, Pipeline) or "scaler" not in first.named_steps:
        return X

    X_out = np.empty_like(X, dtype=float)
    for t, est_t in enumerate(est.estimators_):
        sc = est_t.named_steps.get("scaler", None)
        X_out[..., t] = X[..., t] if sc is None else sc.transform(X[..., t])
    return X_out


def get_decision_values(est, X, raw=False, remove_intercept=True):
    """
    Project trials onto the per-time discriminant axis.

    When raw=True and remove_intercept=True, also subtract w_raw . mu so the
    projection equals w_scaled . z (pure centered projection in raw units).
    """
    ws, bs, means = get_space_params_timewise(est, raw=raw)

    if remove_intercept:
        bs = np.zeros_like(bs)

    X_proj = _apply_scaler_if_needed(est, X, raw=raw)
    is_sliding = est.__class__.__name__ == "SlidingEstimator"

    if is_sliding:
        dec = np.einsum("nft,tf->nt", X_proj, ws) + bs[None, :]
    else:
        dec = np.einsum("nfu,tf->ntu", X_proj, ws) + bs[None, :, None]

    if raw and remove_intercept and means is not None:
        offsets = np.einsum("tf,tf->t", ws, means)
        if is_sliding:
            dec = dec - offsets[None, :]
        else:
            dec = dec - offsets[None, :, None]

    return dec


def get_norms_timewise(est, raw=False):
    ws, _, _ = get_space_params_timewise(est, raw=raw)
    return np.linalg.norm(ws, axis=1)


def postprocess_decision(dec, norms, y=None, signed=False, eps=1e-6):
    """Normalize by weight norm; optionally sign by label (0/1 coding)."""
    norms = norms + eps

    if dec.ndim == 2:      # (n_trials, n_times)
        dec = dec / norms[None, :]
        if signed:
            dec = (2 * np.asarray(y) - 1)[:, None] * dec
    elif dec.ndim == 3:    # (n_trials, n_train_times, n_test_times)
        dec = dec / norms[None, :, None]
        if signed:
            dec = (2 * np.asarray(y) - 1)[:, None, None] * dec
    else:
        raise ValueError(f"Unexpected shape: {dec.shape}")

    return dec
#+end_src

#+RESULTS:

**** Null helpers

#+begin_src jupyter-python
def _dec_from_ws(X_proj, ws, bs=None, remove_intercept=True,
                 means=None, raw=False):
    """Project with given ws; apply mean-offset correction if raw+remove_int."""
    if bs is None or remove_intercept:
        bs = np.zeros(ws.shape[0])
    dec = np.einsum("nfu,tf->ntu", X_proj, ws) + bs[None, :, None]
    if raw and remove_intercept and means is not None:
        offsets = np.einsum("tf,tf->t", ws, means)
        dec = dec - offsets[None, :, None]
    return dec


def shuffle_weights_timewise(ws, rng, preserve_norm=True):
    F = ws.shape[1]
    ws_shuff = np.empty_like(ws)
    for t in range(ws.shape[0]):
        perm = rng.permutation(F)
        ws_shuff[t] = ws[t, perm]
        if preserve_norm:
            n0 = np.linalg.norm(ws[t])
            n1 = np.linalg.norm(ws_shuff[t]) + 1e-12
            ws_shuff[t] *= n0 / n1
    return ws_shuff


def weight_shuffle_null(
    est, X, y, n_shuffles=100, raw=False, signed=False,
    remove_intercept=True, random_state=0, n_jobs=-1,
):
    ws, _, means = get_space_params_timewise(est, raw=raw)
    X_proj = _apply_scaler_if_needed(est, X, raw=raw)
    seeds = np.random.SeedSequence(random_state).generate_state(n_shuffles)

    def _one(seed):
        rng = np.random.default_rng(int(seed))
        ws_s = shuffle_weights_timewise(ws, rng, preserve_norm=True)
        norms_s = np.linalg.norm(ws_s, axis=1)
        dec_s = _dec_from_ws(X_proj, ws_s, remove_intercept=remove_intercept,
                             means=means, raw=raw)
        return postprocess_decision(dec_s, norms_s, y=y, signed=signed)

    out = Parallel(n_jobs=n_jobs, backend="loky")(delayed(_one)(s) for s in seeds)
    return np.stack(out, axis=0)


def label_permutation_null(
    estimator, X_train, y_train, X_test, y_test,
    n_shuffles=100, raw=False, signed=False,
    remove_intercept=True, random_state=0, n_jobs=-1,
):
    seeds = np.random.SeedSequence(random_state).generate_state(n_shuffles)

    def _one(seed):
        rng = np.random.default_rng(int(seed))
        y_perm = rng.permutation(y_train)
        est = clone(estimator)
        est.fit(X_train, y_perm)
        norms = get_norms_timewise(est, raw=raw)
        dec = get_decision_values(est, X_test, raw=raw,
                                  remove_intercept=remove_intercept)
        return postprocess_decision(dec, norms, y=y_test, signed=signed)

    out = Parallel(n_jobs=n_jobs, backend="loky")(delayed(_one)(s) for s in seeds)
    return np.stack(out, axis=0)
#+end_src

#+RESULTS:

**** Dataloader

#+begin_src jupyter-python
def dataloader(
    X, y_df,
    target="sample", stage="Expert", context="DPA",
    correct=False, strata=False,
):
    idx_correct = True
    if correct:
        idx_correct = (y_df.performance == 1) & (
            (y_df.tasks == "DPA") | (y_df.odr_perf == 1)
        )

    idx_context = True
    if target in ("distractor", "odr_choice", "licks"):
        context = "Dual"
        idx_context = (y_df.tasks != "DPA")
    if context in ("DPA", "DualGo", "DualNoGo"):
        idx_context = (y_df.tasks == context)

    m_within = ((y_df.laser == 0) & (y_df.learning == stage)
                & idx_context & idx_correct)
    m_cross = ~m_within

    X_within = X[m_within]
    y_within = y_df.loc[m_within].reset_index(drop=True).copy()
    y_within["labels"] = y_within[target].to_numpy()

    X_cross = X[m_cross]
    y_cross = y_df.loc[m_cross].reset_index(drop=True).copy()
    y_cross["labels"] = y_cross[target].to_numpy()

    print(f"X_within {X_within.shape}  y_within {y_within.shape}")
    print(f"X_cross  {X_cross.shape}  y_cross  {y_cross.shape}")

    if strata:
        strata_vec = (
            y_within['odor_pair'].astype(str) + "_"
            + y_within["tasks"].astype(str) + "_"
            + y_within["day"].astype(str)
        ).to_numpy()
    else:
        strata_vec = y_within[target].astype(str).to_numpy()

    return X_within, y_within, X_cross, y_cross, strata_vec
#+end_src

#+RESULTS:

**** Main CCGD validation loop

#+begin_src jupyter-python
def ccgd_validation(
    X_y_data,
    estimator,
    selector=None,
    cv=None,
    signed=False,
    raw=False,
    remove_intercept=True,
    fit_param_epoch=False,
    bins_epoch=None,
    random_state=0,
    null_type=None,
    n_shuffles=100,
    null_reduction="zscore",
):
    X_within, y_within, X_cross, y_cross, strata = X_y_data
    y_within_labels = y_within["labels"].to_numpy()
    y_cross_labels  = y_cross["labels"].to_numpy()

    has_cross = X_cross.shape[0] > 0
    if not has_cross:
        print("[ccgd_validation] y_cross is empty — skipping cross-condition block.")

    if cv is None:
        cv = StratifiedKFold(n_splits=5, shuffle=True, random_state=random_state)
    splits = list(cv.split(X_within, strata))

    within_probas, within_dfs, y_cv_within = [], [], []
    cross_probas,  cross_dfs               = [], []
    null_info = {"within_mu": [], "within_sd": [],
                 "cross_mu":  [], "cross_sd":  [],
                 "C_per_fold": []}

    for fold, (train_idx, test_idx) in enumerate(splits):
        X_tr, X_te = X_within[train_idx], X_within[test_idx]
        y_tr = y_within_labels[train_idx].copy()
        y_te = y_within_labels[test_idx]
        y_cv_within.append(y_within.iloc[test_idx].reset_index(drop=True))

        est = clone(estimator)

        if fit_param_epoch:
            sel = clone(selector)
            sel.fit(X_tr[..., bins_epoch].mean(-1), y_tr)
            best_C = sel.named_steps["model"].C_[0]
            est.base_estimator.named_steps["model"].C = best_C
            null_info["C_per_fold"].append(best_C)

        est.fit(X_tr, y_tr)
        norms = get_norms_timewise(est, raw=raw)

        # ---- Observed ----
        within_obs = get_decision_values(est, X_te, raw=raw,
                                         remove_intercept=remove_intercept)
        within_obs = postprocess_decision(within_obs, norms, y=y_te, signed=signed)

        if has_cross:
            cross_obs = get_decision_values(est, X_cross, raw=raw,
                                            remove_intercept=remove_intercept)
            cross_obs = postprocess_decision(cross_obs, norms,
                                             y=y_cross_labels, signed=signed)
        else:
            cross_obs = None

        # ---- Null ----
        within_null = cross_null = None
        if null_type == "weight":
            within_null = weight_shuffle_null(
                est, X_te, y_te, n_shuffles=n_shuffles, raw=raw,
                signed=signed, remove_intercept=remove_intercept,
                random_state=random_state + fold,
            )
            if has_cross:
                cross_null = weight_shuffle_null(
                    est, X_cross, y_cross_labels, n_shuffles=n_shuffles,
                    raw=raw, signed=signed, remove_intercept=remove_intercept,
                    random_state=random_state + fold,
                )
        elif null_type == "label":
            within_null = label_permutation_null(
                est, X_tr, y_tr, X_te, y_te,
                n_shuffles=n_shuffles, raw=raw, signed=signed,
                remove_intercept=remove_intercept,
                random_state=random_state + fold,
            )
            if has_cross:
                cross_null = label_permutation_null(
                    est, X_tr, y_tr, X_cross, y_cross_labels,
                    n_shuffles=n_shuffles, raw=raw, signed=signed,
                    remove_intercept=remove_intercept,
                    random_state=random_state + fold,
                )

        # ---- Calibration ----
        def _calibrate(obs, null):
            if obs is None:
                return None, None, None
            if null is None:
                return obs, None, None
            if null_reduction == "zscore":
                return _zscore_against_null(obs, null)
            if null_reduction == "subtract":
                mu = null.mean(0)
                return obs - mu, mu, None
            return obs, None, None

        within_cal, mu_w, sd_w = _calibrate(within_obs, within_null)
        cross_cal,  mu_c, sd_c = _calibrate(cross_obs,  cross_null)

        null_info["within_mu"].append(mu_w)
        null_info["within_sd"].append(sd_w)
        null_info["cross_mu"].append(mu_c)
        null_info["cross_sd"].append(sd_c)

        within_dfs.append(within_cal)
        if has_cross:
            cross_dfs.append(cross_cal)

        # ---- Probas ----
        w_proba = est.predict_proba(X_te)[..., 1]
        w_proba = (y_te[:, None, None] * w_proba
                   + (1 - y_te[:, None, None]) * (1 - w_proba))
        within_probas.append(w_proba)

        if has_cross:
            c_proba = est.predict_proba(X_cross)[..., 1]
            c_proba = (y_cross_labels[:, None, None] * c_proba
                       + (1 - y_cross_labels[:, None, None]) * (1 - c_proba))
            cross_probas.append(c_proba)

    # ---- Aggregate ----
    within_probas = np.vstack(within_probas)
    within_dfs    = np.vstack(within_dfs)
    y_cv_within   = pd.concat(y_cv_within, axis=0, ignore_index=True)

    if has_cross:
        cross_probas = np.mean(np.stack(cross_probas, axis=0), axis=0)
        cross_dfs    = np.mean(np.stack(cross_dfs,    axis=0), axis=0)

        y_cv   = pd.concat([y_cv_within, y_cross], axis=0, ignore_index=True)
        probas = np.vstack([within_probas, cross_probas])
        dfs    = np.vstack([within_dfs,    cross_dfs])
    else:
        y_cv   = y_cv_within
        probas = within_probas
        dfs    = within_dfs

    return probas, dfs, y_cv, null_info
#+end_src

*** Split the following code in a similar way than I did for CCGD

#+begin_src jupyter-python
from sklearn.base import clone
from tqdm import tqdm

def cv_pca(X, y, pca, folds, factors,
           epoch=None, scale=None, stage='Expert', correct='True', context=None,
           group_col=None, show_pbar=True, center_on="trialtime",
           align_folds=False):
    """
    Cross-validated PCA with Procrustes alignment.

    Parameters
    ----------
    X : (n_trials, n_neurons, n_time)
    y : DataFrame aligned with trials
    pca : sklearn PCA estimator
    folds : CV splitter
    factors : condition factors for averaging/anchors
    epoch : time indices for fitting (None = full)
    scale : sklearn scaler (cloned per fold), or None
    stage : learning stage filter
    correct : filter on performance == 1
    context : filter on tasks column, or None for all
    center_on : "trialtime" centers on all trial×time points; else on condition averages
    align_folds : Procrustes-align folds to first fold's anchors
    """
    if isinstance(factors, str):
        factors = [factors]

    def flat_trial_time(A):
        # (n_trials, n_neurons, n_time) -> (n_trials * n_time, n_neurons)
        return A.transpose(0, 2, 1).reshape(-1, A.shape[1])

    # --- Clean / perturbation split ---
    idx_correct = (y.performance == 1) & ((y.odr_perf==1) | (y.tasks=='DPA')) if correct else True

    if context is not None:
        if context=='Dual':
            idx_context = (y.tasks != 'DPA')
        else:
            idx_context = (y.tasks == context)
    else:
        idx_context = True

    m_clean = (y.laser == 0) & (y.learning == stage) & idx_context & idx_correct
    m_pert = ~m_clean

    Xc = X[m_clean]
    yc = y.loc[m_clean].reset_index(drop=True)

    print('Xc', Xc.shape, 'yc', yc.shape)

    X_pert = X[m_pert]
    y_pert = y.loc[m_pert].reset_index(drop=True)

    print('X_pert', X_pert.shape, 'y_pert', y_pert.shape)

    # --- Stratification labels ---
    strata = (
        yc["odor_pair"].astype(str) + "_"
        + yc["day"].astype(str) + "_"
        + yc["tasks"].astype(str)
    ).values
    levels = get_levels(yc, factors)

    # --- Verify all combos present globally (for Procrustes consistency) ---
    combos = list(itertools.product(*[levels[f] for f in factors]))
    for combo in combos:
        if not _combo_mask(yc, factors, combo).any():
            raise ValueError(
                f"Combo {dict(zip(factors, combo))} missing globally; "
                "Procrustes anchors will be inconsistent across folds."
            )

    # --- CV iterator ---
    it = folds.split(Xc, strata, groups=None)
    if show_pbar:
        name = y.mouse.unique()[0] if "mouse" in y.columns else "X"
        it = tqdm(it, total=folds.get_n_splits(Xc, strata), desc=str(name))

    # Accumulators
    # For repeated CV: collect (fold_idx, trial_idx, Z) and average later
    trial_Z_accum = {}  # trial_idx -> list of Z arrays
    Z_cross = []
    w_folds, evr_folds = [], []
    A_ref = None

    for fold_i, (train, test) in enumerate(it):
        X_train, y_train = Xc[train], yc.iloc[train].reset_index(drop=True)
        X_test, y_test = Xc[test], yc.iloc[test].reset_index(drop=True)

        # --- Per-fold scaling ---
        if scale is not None:
            scale_fold = StandardScaler(if_scale=1)
            X_train = scale_fold.fit_transform(X_train)
            X_test = scale_fold.transform(X_test)
            X_pert_scaled = scale_fold.transform(X_pert)
        else:
            X_pert_scaled = X_pert

        # --- Epoch slice for fitting ---
        X_train_epoch = X_train if epoch is None else X_train[..., epoch]

        # --- Condition averages (train, epoch) ---
        X_avg_train, _ = cv_avg_cond(
            X_train_epoch, y_train, factors, levels, drop_missing=False
        )
        X_avg_flat = flat_trial_time(X_avg_train)

        # --- Centering ---
        if center_on == "trialtime":
            X_mean = np.nanmean(
                flat_trial_time(X_train_epoch), axis=0, keepdims=True
            )
        else:
            X_mean = np.nanmean(X_avg_flat, axis=0, keepdims=True)

        # --- Fit PCA ---
        pca_fold = clone(pca)
        pca_fold.fit(X_avg_flat - X_mean)
        w_folds.append(pca_fold.components_)
        evr_folds.append(pca_fold.explained_variance_ratio_)

        # --- Project test (full time) ---
        Z_test = pca_fold.transform(flat_trial_time(X_test) - X_mean)
        Z_test = Z_test.reshape(X_test.shape[0], X_test.shape[-1], -1)

        # --- Project perturbation (full time) ---
        Z_pert_fold = pca_fold.transform(flat_trial_time(X_pert_scaled) - X_mean)
        Z_pert_fold = Z_pert_fold.reshape(
            X_pert_scaled.shape[0], X_pert_scaled.shape[-1], -1
        )

        # --- Procrustes alignment ---
        if align_folds:
            Z_train = pca_fold.transform(
                flat_trial_time(X_train_epoch) - X_mean
            )
            Z_train = Z_train.reshape(
                X_train_epoch.shape[0], X_train_epoch.shape[-1], -1
            )
            A_curr = anchors_from_Z(
                Z_train, y_train, factors, levels, drop_missing=False
            )

            if A_ref is None:
                A_ref = A_curr
            else:
                R, _ = orthogonal_procrustes(A_curr, A_ref)
                Z_test = Z_test @ R
                Z_pert_fold = Z_pert_fold @ R

        # --- Accumulate per-trial projections (handles repeated CV) ---
        for i, orig_idx in enumerate(test):
            trial_Z_accum.setdefault(orig_idx, []).append(Z_test[i])

        Z_cross.append(Z_pert_fold)

    # --- Aggregate clean trials (average over repeats) ---
    sorted_indices = sorted(trial_Z_accum.keys())
    Z_clean = np.stack(
        [np.mean(trial_Z_accum[idx], axis=0) for idx in sorted_indices]
    )
    y_clean = yc.iloc[sorted_indices].reset_index(drop=True)
    print(f"Z_clean: {Z_clean.shape}, y_clean: {y_clean.shape}")

    # --- Aggregate perturbation projections (average over folds) ---
    Zp = np.mean(np.stack(Z_cross, axis=0), axis=0)
    print(f"Z_pert: {Zp.shape}, y_pert: {y_pert.shape}")

    # --- Combine ---
    Z_all = np.concatenate([Z_clean, Zp], axis=0)
    y_all = pd.concat([y_clean, y_pert], ignore_index=True)

    w_folds = np.array(w_folds)
    evr_folds = np.array(evr_folds)

    return Z_all, y_all, w_folds, evr_folds
#+end_src

*** model

#+begin_src jupyter-python
dum = 'log'

mne_estimator = "generalizing"
dum += '_' + mne_estimator

scoring = "overlaps"
dum += '_' + scoring

scaler = 'standard'
if scaler is not None:
    dum += '_' + scaler

correct = False
if correct:
    dum += '_correct'

l1_ratio = 0.0
dum += f'_l1_ratio_{l1_ratio}'

raw = True
if raw:
    dum+= '_raw'

fit_param_epoch=False
if fit_param_epoch:
    dum+= '_fit_epoch'

print(dum)
#+end_src

#+RESULTS:
: log_generalizing_overlaps_standard_l1_ratio_0.0_raw

#+begin_src jupyter-python
n_splits = 5
n_repeats = 1

folds = RepeatedStratifiedKFold(n_splits=n_splits, n_repeats=n_repeats)
# folds = LeaveOneOut()

selector = get_estimator(clf='logcv', scoring='accuracy', mode=None, scaler=scaler, l1_ratio=l1_ratio, n_jobs=-1)
if fit_param_epoch:
    estimator = get_estimator(clf='log', scoring='accuracy', mode=mne_estimator, scaler=scaler, l1_ratio=l1_ratio, n_jobs=-1)
else:
    estimator = get_estimator(clf='logcv', scoring='accuracy', mode=mne_estimator, scaler=scaler, l1_ratio=l1_ratio, n_jobs=-1)
#+end_src

#+RESULTS:

#+begin_src jupyter-python
print(options['mice'])
#+end_src

#+RESULTS:
: ['JawsM01', 'JawsM06', 'JawsM12', 'JawsM15', 'JawsM18', 'ChRM04', 'ChRM23', 'ACCM03', 'ACCM04']

#+begin_src jupyter-python
stages = ['Naive', 'Expert']
targets = ['sample', 'choice', 'test']
contexts = ['all']
bins_epoch = options['bins_CHOICE']

X_mice, y_mice, w_mice = [], [], []
for mouse in options['mice']:
# for mouse in [options['mice'][3]]:

    X_stage, y_stage = [], []

    for stage in stages:
        idx = (y_all.mouse == mouse) & (y_all.learning == stage)

        X_df = X_all[idx]
        y_df = y_all.loc[idx].reset_index(drop=True)

        # Drop all-NaN neurons
        valid_neurons = ~np.all(np.isnan(X_df), axis=(0, 2))
        X_df = X_df[:, valid_neurons, :]

        # Light temporal smoothing
        # X_df = gaussian_filter1d(X_df, sigma=0.3, axis=-1, mode='nearest')

        Z_list, y_list = [], []
        for context in contexts:

            Z_tar, y_tar = [], []
            for target in targets:
                print(f"mouse={mouse} stage={stage} target={target} context={context}")

                X_y_data = dataloader(X_df, y_df, target=target, stage=stage, context=context, correct=correct, strata=True)

                probas, dfs, y_, null_info = ccgd_validation(
                    X_y_data,
                    estimator,
                    selector=selector,
                    cv=folds,
                    fit_param_epoch=fit_param_epoch,
                    bins_epoch=bins_epoch,
                    null_type=None,            # or "weight" / "label"
                    null_reduction="zscore",   # used only if null_type is set
                    n_shuffles=100,
                    raw=raw,
                    signed=False,
                    random_state=0,
                )

                print(probas.shape, dfs.shape)
                Z_ = np.stack([probas, dfs], axis=1)   # (n_trials, 2, n_train, n_test)
                print(Z_.shape)

                y_ = y_.copy()
                y_['target'] = target
                y_['context'] = context

                Z_tar.append(Z_)
                y_tar.append(y_)

            Z_list.append(np.vstack(Z_tar))
            y_list.append(pd.concat(y_tar, axis=0, ignore_index=True))

        Z_mouse = np.vstack(Z_list)
        y_mouse = pd.concat(y_list, axis=0, ignore_index=True)
        y_mouse['stage'] = stage

        print('Z', Z_mouse.shape, 'y', y_mouse.shape)

        X_stage.append(Z_mouse)
        y_stage.append(y_mouse)

    X_mice.append(np.vstack(X_stage))
    y_mice.append(pd.concat(y_stage, axis=0, ignore_index=True))
#+end_src

#+RESULTS:
#+begin_example
mouse=JawsM01 stage=Naive target=sample context=all
X_within (288, 184, 84)  y_within (288, 21)
X_cross  (288, 184, 84)  y_cross  (288, 21)
(576, 84, 84) (576, 84, 84)
(576, 2, 84, 84)
mouse=JawsM01 stage=Naive target=choice context=all
X_within (288, 184, 84)  y_within (288, 21)
X_cross  (288, 184, 84)  y_cross  (288, 21)
(576, 84, 84) (576, 84, 84)
(576, 2, 84, 84)
mouse=JawsM01 stage=Naive target=test context=all
X_within (288, 184, 84)  y_within (288, 21)
X_cross  (288, 184, 84)  y_cross  (288, 21)
(576, 84, 84) (576, 84, 84)
(576, 2, 84, 84)
Z (1728, 2, 84, 84) y (1728, 24)
mouse=JawsM01 stage=Expert target=sample context=all
X_within (96, 184, 84)  y_within (96, 21)
X_cross  (96, 184, 84)  y_cross  (96, 21)
(192, 84, 84) (192, 84, 84)
(192, 2, 84, 84)
mouse=JawsM01 stage=Expert target=choice context=all
X_within (96, 184, 84)  y_within (96, 21)
X_cross  (96, 184, 84)  y_cross  (96, 21)
(192, 84, 84) (192, 84, 84)
(192, 2, 84, 84)
mouse=JawsM01 stage=Expert target=test context=all
X_within (96, 184, 84)  y_within (96, 21)
X_cross  (96, 184, 84)  y_cross  (96, 21)
(192, 84, 84) (192, 84, 84)
(192, 2, 84, 84)
Z (576, 2, 84, 84) y (576, 24)
mouse=JawsM06 stage=Naive target=sample context=all
X_within (288, 201, 84)  y_within (288, 21)
X_cross  (288, 201, 84)  y_cross  (288, 21)
(576, 84, 84) (576, 84, 84)
(576, 2, 84, 84)
mouse=JawsM06 stage=Naive target=choice context=all
X_within (288, 201, 84)  y_within (288, 21)
X_cross  (288, 201, 84)  y_cross  (288, 21)
(576, 84, 84) (576, 84, 84)
(576, 2, 84, 84)
mouse=JawsM06 stage=Naive target=test context=all
X_within (288, 201, 84)  y_within (288, 21)
X_cross  (288, 201, 84)  y_cross  (288, 21)
(576, 84, 84) (576, 84, 84)
(576, 2, 84, 84)
Z (1728, 2, 84, 84) y (1728, 24)
mouse=JawsM06 stage=Expert target=sample context=all
X_within (288, 201, 84)  y_within (288, 21)
X_cross  (288, 201, 84)  y_cross  (288, 21)
(576, 84, 84) (576, 84, 84)
(576, 2, 84, 84)
mouse=JawsM06 stage=Expert target=choice context=all
X_within (288, 201, 84)  y_within (288, 21)
X_cross  (288, 201, 84)  y_cross  (288, 21)
(576, 84, 84) (576, 84, 84)
(576, 2, 84, 84)
mouse=JawsM06 stage=Expert target=test context=all
X_within (288, 201, 84)  y_within (288, 21)
X_cross  (288, 201, 84)  y_cross  (288, 21)
(576, 84, 84) (576, 84, 84)
(576, 2, 84, 84)
Z (1728, 2, 84, 84) y (1728, 24)
mouse=JawsM12 stage=Naive target=sample context=all
X_within (288, 423, 84)  y_within (288, 21)
X_cross  (288, 423, 84)  y_cross  (288, 21)
(576, 84, 84) (576, 84, 84)
(576, 2, 84, 84)
mouse=JawsM12 stage=Naive target=choice context=all
X_within (288, 423, 84)  y_within (288, 21)
X_cross  (288, 423, 84)  y_cross  (288, 21)
(576, 84, 84) (576, 84, 84)
(576, 2, 84, 84)
mouse=JawsM12 stage=Naive target=test context=all
X_within (288, 423, 84)  y_within (288, 21)
X_cross  (288, 423, 84)  y_cross  (288, 21)
(576, 84, 84) (576, 84, 84)
(576, 2, 84, 84)
Z (1728, 2, 84, 84) y (1728, 24)
mouse=JawsM12 stage=Expert target=sample context=all
X_within (192, 423, 84)  y_within (192, 21)
X_cross  (192, 423, 84)  y_cross  (192, 21)
(384, 84, 84) (384, 84, 84)
(384, 2, 84, 84)
mouse=JawsM12 stage=Expert target=choice context=all
X_within (192, 423, 84)  y_within (192, 21)
X_cross  (192, 423, 84)  y_cross  (192, 21)
(384, 84, 84) (384, 84, 84)
(384, 2, 84, 84)
mouse=JawsM12 stage=Expert target=test context=all
X_within (192, 423, 84)  y_within (192, 21)
X_cross  (192, 423, 84)  y_cross  (192, 21)
(384, 84, 84) (384, 84, 84)
(384, 2, 84, 84)
Z (1152, 2, 84, 84) y (1152, 24)
mouse=JawsM15 stage=Naive target=sample context=all
X_within (288, 693, 84)  y_within (288, 21)
X_cross  (288, 693, 84)  y_cross  (288, 21)
(576, 84, 84) (576, 84, 84)
(576, 2, 84, 84)
mouse=JawsM15 stage=Naive target=choice context=all
X_within (288, 693, 84)  y_within (288, 21)
X_cross  (288, 693, 84)  y_cross  (288, 21)
(576, 84, 84) (576, 84, 84)
(576, 2, 84, 84)
mouse=JawsM15 stage=Naive target=test context=all
X_within (288, 693, 84)  y_within (288, 21)
X_cross  (288, 693, 84)  y_cross  (288, 21)
(576, 84, 84) (576, 84, 84)
(576, 2, 84, 84)
Z (1728, 2, 84, 84) y (1728, 24)
mouse=JawsM15 stage=Expert target=sample context=all
X_within (288, 693, 84)  y_within (288, 21)
X_cross  (288, 693, 84)  y_cross  (288, 21)
(576, 84, 84) (576, 84, 84)
(576, 2, 84, 84)
mouse=JawsM15 stage=Expert target=choice context=all
X_within (288, 693, 84)  y_within (288, 21)
X_cross  (288, 693, 84)  y_cross  (288, 21)
(576, 84, 84) (576, 84, 84)
(576, 2, 84, 84)
mouse=JawsM15 stage=Expert target=test context=all
X_within (288, 693, 84)  y_within (288, 21)
X_cross  (288, 693, 84)  y_cross  (288, 21)
(576, 84, 84) (576, 84, 84)
(576, 2, 84, 84)
Z (1728, 2, 84, 84) y (1728, 24)
mouse=JawsM18 stage=Naive target=sample context=all
X_within (288, 444, 84)  y_within (288, 21)
X_cross  (288, 444, 84)  y_cross  (288, 21)
(576, 84, 84) (576, 84, 84)
(576, 2, 84, 84)
mouse=JawsM18 stage=Naive target=choice context=all
X_within (288, 444, 84)  y_within (288, 21)
X_cross  (288, 444, 84)  y_cross  (288, 21)
(576, 84, 84) (576, 84, 84)
(576, 2, 84, 84)
mouse=JawsM18 stage=Naive target=test context=all
X_within (288, 444, 84)  y_within (288, 21)
X_cross  (288, 444, 84)  y_cross  (288, 21)
(576, 84, 84) (576, 84, 84)
(576, 2, 84, 84)
Z (1728, 2, 84, 84) y (1728, 24)
mouse=JawsM18 stage=Expert target=sample context=all
X_within (288, 444, 84)  y_within (288, 21)
X_cross  (288, 444, 84)  y_cross  (288, 21)
(576, 84, 84) (576, 84, 84)
(576, 2, 84, 84)
mouse=JawsM18 stage=Expert target=choice context=all
X_within (288, 444, 84)  y_within (288, 21)
X_cross  (288, 444, 84)  y_cross  (288, 21)
(576, 84, 84) (576, 84, 84)
(576, 2, 84, 84)
mouse=JawsM18 stage=Expert target=test context=all
X_within (288, 444, 84)  y_within (288, 21)
X_cross  (288, 444, 84)  y_cross  (288, 21)
(576, 84, 84) (576, 84, 84)
(576, 2, 84, 84)
Z (1728, 2, 84, 84) y (1728, 24)
mouse=ChRM04 stage=Naive target=sample context=all
X_within (288, 668, 84)  y_within (288, 21)
X_cross  (288, 668, 84)  y_cross  (288, 21)
(576, 84, 84) (576, 84, 84)
(576, 2, 84, 84)
mouse=ChRM04 stage=Naive target=choice context=all
X_within (288, 668, 84)  y_within (288, 21)
X_cross  (288, 668, 84)  y_cross  (288, 21)
(576, 84, 84) (576, 84, 84)
(576, 2, 84, 84)
mouse=ChRM04 stage=Naive target=test context=all
X_within (288, 668, 84)  y_within (288, 21)
X_cross  (288, 668, 84)  y_cross  (288, 21)
(576, 84, 84) (576, 84, 84)
(576, 2, 84, 84)
Z (1728, 2, 84, 84) y (1728, 24)
mouse=ChRM04 stage=Expert target=sample context=all
X_within (288, 668, 84)  y_within (288, 21)
X_cross  (288, 668, 84)  y_cross  (288, 21)
(576, 84, 84) (576, 84, 84)
(576, 2, 84, 84)
mouse=ChRM04 stage=Expert target=choice context=all
X_within (288, 668, 84)  y_within (288, 21)
X_cross  (288, 668, 84)  y_cross  (288, 21)
(576, 84, 84) (576, 84, 84)
(576, 2, 84, 84)
mouse=ChRM04 stage=Expert target=test context=all
X_within (288, 668, 84)  y_within (288, 21)
X_cross  (288, 668, 84)  y_cross  (288, 21)
(576, 84, 84) (576, 84, 84)
(576, 2, 84, 84)
Z (1728, 2, 84, 84) y (1728, 24)
mouse=ChRM23 stage=Naive target=sample context=all
X_within (288, 232, 84)  y_within (288, 21)
X_cross  (288, 232, 84)  y_cross  (288, 21)
(576, 84, 84) (576, 84, 84)
(576, 2, 84, 84)
mouse=ChRM23 stage=Naive target=choice context=all
X_within (288, 232, 84)  y_within (288, 21)
X_cross  (288, 232, 84)  y_cross  (288, 21)
(576, 84, 84) (576, 84, 84)
(576, 2, 84, 84)
mouse=ChRM23 stage=Naive target=test context=all
X_within (288, 232, 84)  y_within (288, 21)
X_cross  (288, 232, 84)  y_cross  (288, 21)
(576, 84, 84) (576, 84, 84)
(576, 2, 84, 84)
Z (1728, 2, 84, 84) y (1728, 24)
mouse=ChRM23 stage=Expert target=sample context=all
X_within (192, 232, 84)  y_within (192, 21)
X_cross  (192, 232, 84)  y_cross  (192, 21)
(384, 84, 84) (384, 84, 84)
(384, 2, 84, 84)
mouse=ChRM23 stage=Expert target=choice context=all
X_within (192, 232, 84)  y_within (192, 21)
X_cross  (192, 232, 84)  y_cross  (192, 21)
(384, 84, 84) (384, 84, 84)
(384, 2, 84, 84)
mouse=ChRM23 stage=Expert target=test context=all
X_within (192, 232, 84)  y_within (192, 21)
X_cross  (192, 232, 84)  y_cross  (192, 21)
(384, 84, 84) (384, 84, 84)
(384, 2, 84, 84)
Z (1152, 2, 84, 84) y (1152, 24)
mouse=ACCM03 stage=Naive target=sample context=all
X_within (576, 361, 84)  y_within (576, 21)
X_cross  (0, 361, 84)  y_cross  (0, 21)
[ccgd_validation] y_cross is empty — skipping cross-condition block.
(576, 84, 84) (576, 84, 84)
(576, 2, 84, 84)
mouse=ACCM03 stage=Naive target=choice context=all
X_within (576, 361, 84)  y_within (576, 21)
X_cross  (0, 361, 84)  y_cross  (0, 21)
[ccgd_validation] y_cross is empty — skipping cross-condition block.
(576, 84, 84) (576, 84, 84)
(576, 2, 84, 84)
mouse=ACCM03 stage=Naive target=test context=all
X_within (576, 361, 84)  y_within (576, 21)
X_cross  (0, 361, 84)  y_cross  (0, 21)
[ccgd_validation] y_cross is empty — skipping cross-condition block.
(576, 84, 84) (576, 84, 84)
(576, 2, 84, 84)
Z (1728, 2, 84, 84) y (1728, 24)
mouse=ACCM03 stage=Expert target=sample context=all
X_within (384, 361, 84)  y_within (384, 21)
X_cross  (0, 361, 84)  y_cross  (0, 21)
[ccgd_validation] y_cross is empty — skipping cross-condition block.
(384, 84, 84) (384, 84, 84)
(384, 2, 84, 84)
mouse=ACCM03 stage=Expert target=choice context=all
X_within (384, 361, 84)  y_within (384, 21)
X_cross  (0, 361, 84)  y_cross  (0, 21)
[ccgd_validation] y_cross is empty — skipping cross-condition block.
(384, 84, 84) (384, 84, 84)
(384, 2, 84, 84)
mouse=ACCM03 stage=Expert target=test context=all
X_within (384, 361, 84)  y_within (384, 21)
X_cross  (0, 361, 84)  y_cross  (0, 21)
[ccgd_validation] y_cross is empty — skipping cross-condition block.
(384, 84, 84) (384, 84, 84)
(384, 2, 84, 84)
Z (1152, 2, 84, 84) y (1152, 24)
mouse=ACCM04 stage=Naive target=sample context=all
X_within (576, 113, 84)  y_within (576, 21)
X_cross  (0, 113, 84)  y_cross  (0, 21)
[ccgd_validation] y_cross is empty — skipping cross-condition block.
(576, 84, 84) (576, 84, 84)
(576, 2, 84, 84)
mouse=ACCM04 stage=Naive target=choice context=all
X_within (576, 113, 84)  y_within (576, 21)
X_cross  (0, 113, 84)  y_cross  (0, 21)
[ccgd_validation] y_cross is empty — skipping cross-condition block.
(576, 84, 84) (576, 84, 84)
(576, 2, 84, 84)
mouse=ACCM04 stage=Naive target=test context=all
X_within (576, 113, 84)  y_within (576, 21)
X_cross  (0, 113, 84)  y_cross  (0, 21)
[ccgd_validation] y_cross is empty — skipping cross-condition block.
(576, 84, 84) (576, 84, 84)
(576, 2, 84, 84)
Z (1728, 2, 84, 84) y (1728, 24)
mouse=ACCM04 stage=Expert target=sample context=all
X_within (384, 113, 84)  y_within (384, 21)
X_cross  (0, 113, 84)  y_cross  (0, 21)
[ccgd_validation] y_cross is empty — skipping cross-condition block.
(384, 84, 84) (384, 84, 84)
(384, 2, 84, 84)
mouse=ACCM04 stage=Expert target=choice context=all
X_within (384, 113, 84)  y_within (384, 21)
X_cross  (0, 113, 84)  y_cross  (0, 21)
[ccgd_validation] y_cross is empty — skipping cross-condition block.
(384, 84, 84) (384, 84, 84)
(384, 2, 84, 84)
mouse=ACCM04 stage=Expert target=test context=all
X_within (384, 113, 84)  y_within (384, 21)
X_cross  (0, 113, 84)  y_cross  (0, 21)
[ccgd_validation] y_cross is empty — skipping cross-condition block.
(384, 84, 84) (384, 84, 84)
(384, 2, 84, 84)
Z (1152, 2, 84, 84) y (1152, 24)
#+end_example

#+begin_src jupyter-python
X_single = np.vstack(X_mice)
y_single = pd.concat(y_mice, ignore_index=True)
print(X_single.shape, y_single.shape)
#+end_src

#+RESULTS:
: (27648, 2, 84, 84) (27648, 24)

#+begin_src jupyter-python
pkl_save(X_single, 'X_' + dum, path="../data/overlaps/")
pkl_save(y_single, 'labels_' + dum, path="../data/overlaps/")
#+end_src

#+RESULTS:
: saving to ../data/overlaps//X_log_generalizing_overlaps_standard_l1_ratio_0.0.pkl
: saving to ../data/overlaps//labels_log_generalizing_overlaps_standard_l1_ratio_0.0.pkl

* Visualizations
** Loading model results

 #+begin_src jupyter-python
X_single = pkl_load('X_' + dum, path="../data/overlaps/")
y_single = pkl_load( 'labels_' + dum, path="../data/overlaps/")
#+end_src

#+RESULTS:
: loading from ../data/overlaps//X_log_generalizing_overlaps_standard_l1_ratio_0.0.pkl
: loading from ../data/overlaps//labels_log_generalizing_overlaps_standard_l1_ratio_0.0.pkl

#+begin_src jupyter-python
print(X_single.shape, y_single.shape)
#+end_src

#+RESULTS:
: (27648, 2, 84, 84) (27648, 24)

** Trajectories

#+begin_src jupyter-python
X_epoch = np.diagonal(X_single, axis1=-2, axis2=-1)[:, 1]
# X_epoch = X_single[..., options['bins_CHOICE']].mean(-1)[:, 1]
X_epoch = X_single[..., options['bins_TEST'], :].mean(-2)[:, 1]
# X_epoch -= X_epoch[..., options['bins_BL']].mean(-1, keepdims=True)
print(X_epoch.shape)
#+end_src

#+RESULTS:
: (27648, 84)

#+begin_src jupyter-python
n_comp = 3
laser = 0
i_mouse= -1

idx_context = True # (y_single.context!='DPA')
idx_correct = (y_single.laser==0)  & (y_single.performance==1) & ((y_single.tasks=='DPA') | (y_single.odr_perf==1)) # & (y_single.choice==1)
idx_mouse = True
if i_mouse !=-1:
    idx_mouse = (y_single.mouse==options['mice'][i_mouse])
    # idx_mouse = y_single.mouse.isin(options['new_mice'])
    print(y_single[idx_mouse].mouse.unique())
#+end_src

#+RESULTS:

#+begin_src jupyter-python
def plot_mat(X, ax, vmin=-1, vmax=1, palette='bwr'):
  im = ax.imshow(
    X,
    interpolation=None,
    origin="lower",
    cmap=palette,
    extent=[0, 14, 0, 14],
    vmin=vmin,
    vmax=vmax,
  )

  add_vdashed(ax,1)
  ax.set_xlim([2, 12])
  ax.set_xticks([2, 4, 6, 8, 10, 12])
  ax.set_ylim([2, 12])
  ax.set_yticks([2, 4, 6, 8, 10, 12])

  ax.set_xlabel("Testing Time (s)")
  ax.set_ylabel("Training Time (s)")
  return im
#+end_src

#+RESULTS:

#+begin_src jupyter-python
fig, ax = plt.subplots(nrows=1, ncols=n_comp, figsize=(n_comp*width, height), sharey=1)
xtime = np.linspace(0, 14, X_single.shape[-1])

idx_stage = (y_single.learning=='Expert')
idx_target = (y_single.target=='sample')

palette = sns.color_palette("muted")
color = [palette[3], palette[0], palette[2]]

tasks = ['DPA', 'DualGo', 'DualNoGo']
targets = y_single.target.unique()
labels = y_single.labels.unique()

for i, task in enumerate(tasks):
        mask = (y_single.tasks==task) & (y_single.labels==1) & idx_mouse & idx_stage & idx_correct & idx_context & idx_target

        X_sel = X_single[mask].mean(0)[1]
        vmin, vmax =np.percentile(X_sel, [5, 95])
        plot_mat(X_sel, ax[i], vmin=vmin, vmax=vmax, palette='jet')

        ax[i].set_xlim([0, 14])
        ax[i].set_ylim([0, 14])
        ax[i].set_xticks((np.linspace(0, 14, 8)[::2]))

plt.savefig('./figures/overlaps/_%s_tasks.svg' % dum)
plt.show()
#+end_src

#+RESULTS:
[[file:./figures/overlaps/figure_32.png]]

#+begin_src jupyter-python
fig, ax = plt.subplots(nrows=1, ncols=n_comp, figsize=(n_comp*width, height), sharey=1)
xtime = np.linspace(0, 14, X_single.shape[-1])

idx_stage = (y_single.learning=='Naive')
idx_target = (y_single.target=='choice')

palette = sns.color_palette("muted")
color = [palette[3], palette[0], palette[2]]

tasks = ['DPA', 'DualGo', 'DualNoGo']
targets = y_single.target.unique()
labels = y_single.labels.unique()

for i, task in enumerate(tasks):
        mask = (y_single.tasks==task) & (y_single.labels==1) & idx_mouse & idx_stage & idx_correct & idx_context & idx_target

        X_sel = X_single[mask].mean(0)[1]
        vmin, vmax =np.percentile(X_sel, [5, 95])
        plot_mat(X_sel, ax[i], vmin=vmin, vmax=vmax, palette='jet')

        ax[i].set_xlim([0, 14])
        ax[i].set_ylim([0, 14])
        ax[i].set_xticks((np.linspace(0, 14, 8)[::2]))

plt.savefig('./figures/overlaps/_%s_tasks.svg' % dum)
plt.show()
#+end_src

#+RESULTS:
[[file:./figures/overlaps/figure_33.png]]

#+begin_src jupyter-python
fig, ax = plt.subplots(nrows=1, ncols=n_comp, figsize=(n_comp*width, height), sharey=1)
xtime = np.linspace(0, 14, X_single.shape[-1])

idx_stage = (y_single.learning=='Expert')
idx_target = (y_single.target=='test')

palette = sns.color_palette("muted")
color = [palette[3], palette[0], palette[2]]

tasks = ['DPA', 'DualGo', 'DualNoGo']
targets = y_single.target.unique()
labels = y_single.labels.unique()

for i, task in enumerate(tasks):
        mask = (y_single.tasks==task) & (y_single.labels==1) & idx_mouse & idx_stage & idx_correct & idx_context & idx_target

        X_sel = X_single[mask].mean(0)[1]
        vmin, vmax =np.percentile(X_sel, [5, 95])
        plot_mat(X_sel, ax[i], vmin=vmin, vmax=vmax, palette='jet')

        ax[i].set_xlim([0, 14])
        ax[i].set_ylim([0, 14])
        ax[i].set_xticks((np.linspace(0, 14, 8)[::2]))

plt.savefig('./figures/overlaps/_%s_tasks.svg' % dum)
plt.show()
#+end_src

#+RESULTS:
[[file:./figures/overlaps/figure_34.png]]

 #+begin_src jupyter-python
fig, ax = plt.subplots(nrows=1, ncols=n_comp, figsize=(n_comp*width, height), sharey=1)
xtime = np.linspace(0, 14, X_single.shape[-1])

idx_stage = (y_single.learning=='Naive')

palette = sns.color_palette("muted")
color = [palette[3], palette[0], palette[2]]

tasks = ['DPA', 'DualGo', 'DualNoGo']
targets = y_single.target.unique()
labels = y_single.labels.unique()

for k, target in enumerate(targets):
    for i, task in enumerate(tasks):
        for j, label in enumerate(labels):

            mask = (y_single.tasks==task) & (y_single.target==target) & (y_single.labels==label) & idx_mouse & idx_stage & idx_correct & idx_context

            X_sel = X_epoch[mask]
            y_avg = X_sel.mean(0)
            y_sem = X_sel.std(0) / np.sqrt(X_sel.shape[0])

            ax[k].plot(xtime, y_avg, color=color[i], label=task if j==1 else None)
            ax[k].fill_between(xtime, y_avg-y_sem, y_avg+y_sem, color=color[i], alpha=0.2)
            ax[k].axhline(0.0, ls='--', color='k')
            ax[k].set_xlabel('Time')
            add_vlines(ax[k], if_dpa=0)

            ax[k].legend(fontsize=12, frameon=0, loc='best')
            ax[k].set_xlim([0, 14])
            # ax[k].set_ylim([0.4, 1.0])
            ax[k].set_xticks((np.linspace(0, 14, 8)[::2]))

            ax[k].set_ylabel(f'{target} Overlap')

plt.savefig('./figures/overlaps/_%s_tasks.svg' % dum)
plt.show()
#+end_src

#+RESULTS:
[[file:./figures/overlaps/figure_35.png]]

#+begin_src jupyter-python
fig, ax = plt.subplots(nrows=1, ncols=n_comp, figsize=(n_comp*width, height), sharey=1)
xtime = np.linspace(0, 14, X_single.shape[-1])

idx_stage = (y_single.learning=='Expert')

palette = sns.color_palette("muted")
color = [palette[3], palette[0], palette[2]]

tasks = ['DPA', 'DualGo', 'DualNoGo']
targets = y_single.target.unique()
labels = y_single.labels.unique()

for k, target in enumerate(targets):
    for i, task in enumerate(tasks):
        for j, label in enumerate(labels):

            mask = (y_single.tasks==task) & (y_single.target==target) & (y_single.labels==label) & idx_mouse & idx_stage & idx_correct & idx_context

            X_sel = X_epoch[mask]
            y_avg = X_sel.mean(0)
            y_sem = X_sel.std(0) / np.sqrt(X_sel.shape[0])

            ax[k].plot(xtime, y_avg, color=color[i], label=task if j==1 else None)
            ax[k].fill_between(xtime, y_avg-y_sem, y_avg+y_sem, color=color[i], alpha=0.2)
            ax[k].axhline(0.0, ls='--', color='k')
            ax[k].set_xlabel('Time')
            add_vlines(ax[k], if_dpa=0)

            ax[k].legend(fontsize=12, frameon=0, loc='best')
            ax[k].set_xlim([0, 14])
            # ax[k].set_ylim([0.4, 1.0])
            ax[k].set_xticks((np.linspace(0, 14, 8)[::2]))

            ax[k].set_ylabel(f'{target} Overlap')

plt.savefig('./figures/overlaps/_%s_tasks.svg' % dum)
plt.show()
#+end_src

#+RESULTS:
[[file:./figures/overlaps/figure_36.png]]

#+begin_src jupyter-python
fig, ax = plt.subplots(nrows=1, ncols=n_comp, figsize=(n_comp*width, height), sharey=1)

palette = sns.color_palette("muted")
color = [palette[4], palette[5]]

targets = y_single.target.unique()
tasks = y_single.tasks.unique()
stages = y_single.stage.unique()

for k, target in enumerate(targets):
    for i, stage in enumerate(stages):
        for j, label in enumerate(labels):
           mask = (y_single.stage==stage) & (y_single.target==target)  & (y_single.labels==label) & idx_mouse & idx_correct & idx_context & (y_single.tasks=='DPA')

           X_sel = X_epoch[mask]

           y_avg = X_sel.mean(0)
           y_sem = X_sel.std(0) / np.sqrt(X_sel.shape[0])  # SEM

           ax[k].plot(xtime, y_avg, color=color[i], label=stage if j==1 else None)

           ax[k].fill_between(xtime, y_avg-y_sem, y_avg+y_sem, color=color[i], alpha=0.2)
           ax[k].axhline(0.0, ls='--', color='k')
           ax[k].set_xlabel('Time')
           add_vlines(ax[k], if_dpa=0)

           ax[k].legend(fontsize=12, frameon=0, loc='best')
           ax[k].set_xlim([0, 14])
           ax[k].set_xticks((np.linspace(0, 14, 8)[::2]))


           ax[k].set_ylabel(f'{target} overlap')


plt.savefig('./figures/single/pca_traj_%s_tasks.svg' % dum)
plt.show()
#+end_src

#+RESULTS:
[[file:./figures/overlaps/figure_37.png]]

#+begin_src jupyter-python

#+end_src

#+RESULTS:

** Visualisation

I want to plot:

- Per-neuron loading heatmap (neurons × {sample, lick, test} PCA axes), sorted by dominant loading. Rows, neurons (pooled across mice, n = [Z]); columns, three axes; color, z-scored loading weight. Marginal bar plot shows the number of neurons with each dominant loading. Inset: variance explained per PCA axis.

I'll focus on the core request. Quick clarifying notes before code — tell me if you want different choices:

*Assumptions / choices I'm making*
1. "Per-neuron loading" = the decoder weight vector $w$ per {sample, choice, test} axis, per neuron, /in raw space/ (=raw=True=, which matches your current run). These are not PCA loadings in the strict sense; they are the linear discriminant directions from the time-generalized logistic classifier. If you want actual PCA axes, say so and I'll swap.
2. One weight per (neuron × axis). Your decoder is time-resolved, so I'll collapse time by averaging $w_t$ over a task-relevant epoch (choice period bins, =options['bins_CHOICE']=). Change =EPOCH_BINS= if you want TEST, DIST, or a specific delay window.
3. Pool neurons across mice by concatenation. Each mouse contributes a disjoint set of rows. I'll keep a =mouse= index vector in case you want to split later.
4. z-score each column (axis) across neurons before plotting.
5. "Dominant loading" = axis with max |z| per neuron.
6. "Variance explained per PCA axis" inset: since these are decoder axes, I'll report instead the fraction of total squared weight carried by each axis, $\|w_k\|^2 / \sum_j \|w_j\|^2$. If you want true PCA var-explained, I need to run PCA on the pooled activity — different pipeline.
7. Your run did not save the fitted estimators (=ccgd_validation= returns projections but not the models). To get per-neuron weights I need to refit once per (mouse, target) on all within-condition data, no CV. Let me know if you'd rather average fold weights from a rerun.

Below assumes I refit a single estimator per (mouse, stage, target) to extract weights. This is cheap.

*Code*

#+begin_src jupyter-python
# ---- Config ----
AXES       = ["sample", "choice", "test"]
STAGE      = "Expert"          # or "Naive"
CONTEXT    = "all"
EPOCH_BINS = options["bins_CHOICE"]  # time window to collapse w_t
RAW        = True
REMOVE_INT = True

def fit_axis_weights(X, y_df, target, stage, context, epoch_bins):
    """Refit one estimator on full within-condition data; return mean w over epoch.
    Returns (w_epoch: (n_neurons,), valid_mask: (n_neurons_full,))."""
    Xw, yw, _, _, _ = dataloader(X, y_df, target=target, stage=stage,
                                 context=context, correct=False, strata=False)
    est = get_estimator(clf="logcv", scoring="accuracy", mode="generalizing",
                        scaler=scaler, l1_ratio=l1_ratio, n_jobs=-1)
    est.fit(Xw, yw["labels"].to_numpy())
    ws, _, _ = get_space_params_timewise(est, raw=RAW)  # (T, F)
    return ws[epoch_bins].mean(0)                       # (F,)

# ---- Collect per-neuron weights across mice ----
rows = []   # list of dicts: one per neuron
for mouse in options["mice"]:
    idx_m = (y_all.mouse == mouse) & (y_all.learning == STAGE)
    X_m = X_all[idx_m]
    y_m = y_all.loc[idx_m].reset_index(drop=True)

    valid = ~np.all(np.isnan(X_m), axis=(0, 2))  # (n_neurons_full,)
    X_m = X_m[:, valid, :]
    if X_m.shape[0] == 0 or X_m.shape[1] == 0:
        continue

    w_by_axis = {}
    for ax_name in AXES:
        try:
            w = fit_axis_weights(X_m, y_m, ax_name, STAGE, CONTEXT, EPOCH_BINS)
        except Exception as e:
            print(f"[skip] {mouse} {ax_name}: {e}")
            w = np.full(X_m.shape[1], np.nan)
        w_by_axis[ax_name] = w

    for i in range(X_m.shape[1]):
        rows.append({
            "mouse": mouse,
            "neuron": i,
            **{ax: w_by_axis[ax][i] for ax in AXES},
        })

W_df = pd.DataFrame(rows)
print(W_df.shape, W_df.head())
#+end_src

#+RESULTS:
#+begin_example
X_within (96, 184, 84)  y_within (96, 21)
X_cross  (96, 184, 84)  y_cross  (96, 21)
X_within (96, 184, 84)  y_within (96, 21)
X_cross  (96, 184, 84)  y_cross  (96, 21)
X_within (96, 184, 84)  y_within (96, 21)
X_cross  (96, 184, 84)  y_cross  (96, 21)
X_within (288, 201, 84)  y_within (288, 21)
X_cross  (288, 201, 84)  y_cross  (288, 21)
X_within (288, 201, 84)  y_within (288, 21)
X_cross  (288, 201, 84)  y_cross  (288, 21)
X_within (288, 201, 84)  y_within (288, 21)
X_cross  (288, 201, 84)  y_cross  (288, 21)
X_within (192, 423, 84)  y_within (192, 21)
X_cross  (192, 423, 84)  y_cross  (192, 21)
X_within (192, 423, 84)  y_within (192, 21)
X_cross  (192, 423, 84)  y_cross  (192, 21)
X_within (192, 423, 84)  y_within (192, 21)
X_cross  (192, 423, 84)  y_cross  (192, 21)
X_within (288, 693, 84)  y_within (288, 21)
X_cross  (288, 693, 84)  y_cross  (288, 21)
X_within (288, 693, 84)  y_within (288, 21)
X_cross  (288, 693, 84)  y_cross  (288, 21)
X_within (288, 693, 84)  y_within (288, 21)
X_cross  (288, 693, 84)  y_cross  (288, 21)
X_within (288, 444, 84)  y_within (288, 21)
X_cross  (288, 444, 84)  y_cross  (288, 21)
X_within (288, 444, 84)  y_within (288, 21)
X_cross  (288, 444, 84)  y_cross  (288, 21)
X_within (288, 444, 84)  y_within (288, 21)
X_cross  (288, 444, 84)  y_cross  (288, 21)
X_within (288, 668, 84)  y_within (288, 21)
X_cross  (288, 668, 84)  y_cross  (288, 21)
X_within (288, 668, 84)  y_within (288, 21)
X_cross  (288, 668, 84)  y_cross  (288, 21)
X_within (288, 668, 84)  y_within (288, 21)
X_cross  (288, 668, 84)  y_cross  (288, 21)
X_within (192, 232, 84)  y_within (192, 21)
X_cross  (192, 232, 84)  y_cross  (192, 21)
X_within (192, 232, 84)  y_within (192, 21)
X_cross  (192, 232, 84)  y_cross  (192, 21)
X_within (192, 232, 84)  y_within (192, 21)
X_cross  (192, 232, 84)  y_cross  (192, 21)
X_within (384, 361, 84)  y_within (384, 21)
X_cross  (0, 361, 84)  y_cross  (0, 21)
X_within (384, 361, 84)  y_within (384, 21)
X_cross  (0, 361, 84)  y_cross  (0, 21)
X_within (384, 361, 84)  y_within (384, 21)
X_cross  (0, 361, 84)  y_cross  (0, 21)
X_within (384, 113, 84)  y_within (384, 21)
X_cross  (0, 113, 84)  y_cross  (0, 21)
X_within (384, 113, 84)  y_within (384, 21)
X_cross  (0, 113, 84)  y_cross  (0, 21)
X_within (384, 113, 84)  y_within (384, 21)
X_cross  (0, 113, 84)  y_cross  (0, 21)
(3319, 5)      mouse  neuron    sample    choice      test
0  JawsM01       0  0.004034 -0.011201 -0.097235
1  JawsM01       1  0.390966 -0.037583  0.038989
2  JawsM01       2 -1.262015 -0.018994 -0.258879
3  JawsM01       3 -1.046284 -0.086843 -0.321326
4  JawsM01       4  1.518381  0.038839  0.012242
#+end_example

#+begin_src jupyter-python
# ---- z-score per axis, find dominant axis ----
W = W_df[AXES].to_numpy().astype(float)                 # (N, 3)
W_z = (W - np.nanmean(W, 0)) / (np.nanstd(W, 0) + 1e-12)

dominant = np.nanargmax(np.abs(W_z), axis=1)            # (N,)
dom_strength = np.nanmax(np.abs(W_z), axis=1)

# sort: first by dominant axis, then within axis by signed z on that axis
sort_key = np.lexsort((-W_z[np.arange(len(W_z)), dominant], dominant))
W_z_sorted = W_z[sort_key]
dom_sorted = dominant[sort_key]

# axis "variance" = fraction of squared weight (not PCA var-explained)
ax_energy = np.nansum(W**2, axis=0)
ax_frac   = ax_energy / ax_energy.sum()
#+end_src

#+RESULTS:

#+begin_src jupyter-python
# ---- Plot ----
from matplotlib.gridspec import GridSpec

fig = plt.figure(figsize=(6, 7))
gs  = GridSpec(1, 2, width_ratios=[4, 1], wspace=0.05, figure=fig)

# heatmap
ax_h = fig.add_subplot(gs[0, 0])
vmax = np.nanpercentile(np.abs(W_z_sorted), 99)
im = ax_h.imshow(W_z_sorted, aspect="auto", cmap="RdBu_r",
                 vmin=-vmax, vmax=vmax, interpolation="nearest")
ax_h.set_xticks(range(len(AXES)))
ax_h.set_xticklabels(AXES)
ax_h.set_ylabel(f"Neurons (n = {len(W_z_sorted)}), sorted by dominant axis")
ax_h.set_title(f"Decoder loadings, {STAGE}, epoch=CHOICE")
cbar = fig.colorbar(im, ax=ax_h, fraction=0.03, pad=0.02)
cbar.set_label("z-scored weight")

# marginal bar: count per dominant axis
ax_b = fig.add_subplot(gs[0, 1], sharey=ax_h)
# build per-row count in the sorted order: one bar per axis showing counts
counts = np.array([(dom_sorted == k).sum() for k in range(len(AXES))])
# draw as horizontal colored bands instead (more informative with rows)
palette = sns.color_palette("muted", n_colors=len(AXES))
for k in range(len(AXES)):
    ys = np.where(dom_sorted == k)[0]
    if len(ys):
        ax_b.barh(ys.mean(), counts[k], height=len(ys),
                  color=palette[k], alpha=0.7, label=f"{AXES[k]} (n={counts[k]})")
ax_b.set_xlabel("count")
ax_b.legend(frameon=False, fontsize=8, loc="lower right")
ax_b.tick_params(left=False, labelleft=False)

# inset: axis energy fraction
ax_i = fig.add_axes([0.62, 0.72, 0.18, 0.15])
ax_i.bar(AXES, ax_frac, color=palette)
ax_i.set_ylabel(r"$\|w\|^2$ frac.")
ax_i.set_ylim(0, 1)
for s in ("top", "right"):
    ax_i.spines[s].set_visible(False)

plt.savefig(f"./figures/overlaps/loadings_heatmap_{STAGE}_{dum}.svg",
            bbox_inches="tight")
plt.show()
#+end_src

#+RESULTS:
[[file:./figures/overlaps/figure_41.png]]

*Sanity checks to run after*
- Confirm total neuron count equals sum over mice of post-valid-mask neurons.
- Confirm dominant-axis bar counts sum to N.
- If one axis dominates almost all rows, the heatmap will look trivial — consider z-scoring per row instead, or per (mouse, axis) to remove between-mouse scale.

*What I did not do*
- True PCA var-explained (different object). Say the word if you want PCA on pooled activity with axes aligned to each target via regression.
- Null / significance thresholding of weights (e.g., only plot rows with any |z|>2). Easy to add.
- Splitting by Naive vs Expert side-by-side. Easy: wrap the whole block in a stage loop and plot two panels.

***



** Individuals

Here's a cleaner, unified version of the /Individuals/ section. I factored out the repeated logic (stage normalization, per-mouse aggregation, delta construction, correlation plotting) into small helpers, then rebuilt all 4 figures on top of them.

#+begin_src jupyter-python
# ============================================================
# Individuals — refactored
# ============================================================
import numpy as np
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns
from scipy.stats import pearsonr, spearmanr

# ------------------------------------------------------------
# Config
# ------------------------------------------------------------
DELAY_BINS = options["bins_LD"]
TASKS      = ["DPA", "DualGo", "DualNoGo"]
FIG_DIR    = "./figures/single"

# ------------------------------------------------------------
# Helpers
# ------------------------------------------------------------
def normalize_stage(s):
    """Map learning labels to {'Naive','Expert'}."""
    if np.issubdtype(s.dtype, np.number):
        return s.map({0: "Naive", 1: "Expert"})
    return (s.astype(str).str.strip().str.lower()
             .map({"naive": "Naive", "expert": "Expert",
                   "0": "Naive", "1": "Expert"}))


def correct_trials(df):
    """Standard correct, non-laser trial mask."""
    return (df.laser == 0) & (df.performance == 1) & (
        (df.tasks == "DPA") | (df.odr_perf == 1)
    )


def attach_delay_value(X_epoch, y_df, delay_bins=DELAY_BINS, trial_mask=None):
    """Return y_df with a per-trial 'value' = mean trajectory in delay_bins."""
    d = y_df.reset_index(drop=True).copy()
    d["traj"] = list(X_epoch)
    if trial_mask is not None:
        d = d[trial_mask.values if isinstance(trial_mask, pd.Series)
              else trial_mask].copy()
    d["value"] = d["traj"].apply(lambda t: np.nanmean(np.asarray(t)[delay_bins]))
    d["stage"] = normalize_stage(d["learning"])
    d = d[d["stage"].isin(["Naive", "Expert"])]
    return d


def pivot_delta(df, index_cols, value_col):
    """Pivot Naive/Expert and compute delta = Expert - Naive."""
    w = (df.pivot_table(index=index_cols, columns="stage",
                        values=value_col, aggfunc="mean")
           .dropna(subset=["Naive", "Expert"])
           .reset_index())
    w["delta"] = w["Expert"] - w["Naive"]
    return w


def corr_annotate(ax, x, y):
    """Fit line + Pearson/Spearman annotation."""
    ok = np.isfinite(x) & np.isfinite(y)
    x, y = x[ok], y[ok]
    if len(x) < 3 or np.std(x) == 0 or np.std(y) == 0:
        return
    r, p   = pearsonr(x, y)
    rs, ps = spearmanr(x, y)
    b1, b0 = np.polyfit(x, y, 1)
    xs = np.linspace(x.min(), x.max(), 50)
    ax.plot(xs, b0 + b1 * xs, "k", lw=1)
    ax.set_title(ax.get_title() +
                 f"\nr={r:.2f} p={p:.2g} | ρ={rs:.2f} p={ps:.2g}",
                 fontsize=10)


def style_axes(ax, xlabel, ylabel, identity=False):
    ax.axhline(0, ls="--", color="0.4", lw=1)
    ax.axvline(0, ls="--", color="0.4", lw=1)
    ax.set_xlabel(xlabel); ax.set_ylabel(ylabel)
    if identity:
        xl, yl = ax.get_xlim(), ax.get_ylim()
        lo, hi = min(xl[0], yl[0]), max(xl[1], yl[1])
        ax.plot([lo, hi], [lo, hi], "--", color="0.6", lw=1)
        ax.set_xlim(lo, hi); ax.set_ylim(lo, hi)


# ------------------------------------------------------------
# 1. Per-mouse delay-overlap table (Naive vs Expert) by task/context
# ------------------------------------------------------------
d_all = attach_delay_value(X_epoch, y_single,
                           trial_mask=correct_trials(y_single))

wide = pivot_delta(
    d_all, index_cols=["mouse", "context", "tasks"], value_col="value"
).rename(columns={"tasks": "task", "delta": "d_overlap"})




#+end_src

#+RESULTS:

Key changes:

1. /Stage normalization/ and /correct-trial mask/ are single helpers (=normalize_stage=, =correct_trials=).
2. /Delay value extraction/ is one function (=attach_delay_value=) replacing 3 near-duplicate loops.
3. /Δ computation/ via a generic =pivot_delta(df, index_cols, value_col)=.
4. /Correlation + regression annotation/ and /axis styling/ are reusable (=corr_annotate=, =style_axes=).
5. All four figures share the same scaffolding; only the scatter variables change.
6. File naming is consistent via =FIG_DIR= and =dum=.

If you want, I can also add bootstrap CIs on the regression lines or switch to partial correlations controlling for mouse identity.

#+begin_src jupyter-python
# ------------------------------------------------------------
# Figure 1: Naive vs Expert scatter, one panel per context
# ------------------------------------------------------------
def fig_naive_vs_expert(wide, col, col_order, fname):
    g = sns.FacetGrid(wide, col=col, col_order=col_order,
                      height=height, aspect=1.0, sharex=True, sharey=True)

    def _plot(data, **kw):
        ax = plt.gca()
        sns.scatterplot(data=data, x="Naive", y="Expert",
                        hue="mouse", style="task",
                        s=80, edgecolor="black", linewidth=0.4,
                        legend=False, ax=ax)

    g.map_dataframe(_plot)

    for ax, key in zip(g.axes.flat, col_order):
        sub = wide[wide[col] == key]
        style_axes(ax, "Naive", "Expert", identity=True)
        ax.set_title(str(key))
        corr_annotate(ax, sub["Naive"].values, sub["Expert"].values)

    g.fig.tight_layout()
    g.fig.savefig(f"{FIG_DIR}/{fname}_{dum}.svg")
    plt.show()


fig_naive_vs_expert(
    wide, col="context",
    col_order=sorted(wide["context"].unique()),
    fname="scatter_delay_by_context",
)
#+end_src

#+RESULTS:
[[file:./figures/overlaps/figure_40.png]]

#+begin_src jupyter-python
# ------------------------------------------------------------
# Figure 4: Naive vs Expert (per-mouse and per-(mouse,sample))
# ------------------------------------------------------------
# per-mouse (collapse across tasks/contexts)
ov_mouse = (wide.groupby("mouse", as_index=False)[["Naive", "Expert"]].mean())

# per-(mouse, sample) on DPA correct trials only
d_sample = attach_delay_value(
    X_epoch, y_single,
    trial_mask=(y_single.laser == 0)
               & (y_single.tasks == "DPA")
               & (y_single.performance == 1),
)
wide_sample = (d_sample.pivot_table(index=["mouse", "sample"],
                                    columns="stage", values="value")
                        .dropna(subset=["Naive", "Expert"])
                        .reset_index())

fig, axes = plt.subplots(1, 2, figsize=(2 * height, height))
for ax, data, style, title in [
    (axes[0], ov_mouse, None, "per mouse"),
    (axes[1], wide_sample, "sample", "per (mouse, sample)"),
]:
    sns.scatterplot(data=data, x="Naive", y="Expert",
                    hue="mouse", style=style,
                    s=100, edgecolor="black", linewidth=0.5,
                    legend=False, ax=ax)
    ax.set_title(title)
    style_axes(ax, "Naive", "Expert", identity=True)
    corr_annotate(ax, data["Naive"].values, data["Expert"].values)

fig.tight_layout()
fig.savefig(f"{FIG_DIR}/scatter_choice_overlap_naive_vs_expert_combined_{dum}.svg")
plt.show()
#+end_src

#+RESULTS:
[[file:./figures/overlaps/figure_41.png]]

#+begin_src jupyter-python
# ------------------------------------------------------------
# Figure 3: Naive vs Expert overlap per task (one panel per task)
# ------------------------------------------------------------
fig, axes = plt.subplots(1, len(TASKS),
                         figsize=(len(TASKS) * height, height),
                         sharex=True, sharey=True)
for ax, t in zip(axes, TASKS):
    sub = wide[wide["task"] == t]
    sns.scatterplot(data=sub, x="Naive", y="Expert",
                    hue="mouse", s=100, edgecolor="black",
                    linewidth=0.5, legend=False, ax=ax)
    ax.set_title(t)
    style_axes(ax, "Overlap Naive", "Overlap Expert", identity=True)
    corr_annotate(ax, sub["Naive"].values, sub["Expert"].values)

fig.tight_layout()
fig.savefig(f"{FIG_DIR}/scatter_naive_expert_overlap_bytask_{dum}.svg")
plt.show()
#+end_src

#+RESULTS:
[[file:./figures/overlaps/figure_42.png]]


#+begin_src jupyter-python
# ------------------------------------------------------------
# Figure 2: Δ overlap  vs  Δ performance  per task
# ------------------------------------------------------------
d_perf = []
for t in TASKS:
    sub = y_single[y_single["tasks"] == t].copy()
    sub["stage"] = normalize_stage(sub["learning"])
    sub = sub[(sub["laser"] == 0) & sub["stage"].isin(["Naive", "Expert"])]
    perf_col = "performance" if t == "DPA" else "odr_perf"
    w = pivot_delta(sub, index_cols=["mouse"], value_col=perf_col)
    w["task"] = t
    w = w.rename(columns={"delta": "d_perf"})
    d_perf.append(w[["mouse", "task", "d_perf"]])
d_perf = pd.concat(d_perf, ignore_index=True)

d_ov_task = (wide.groupby(["mouse", "task"], as_index=False)["d_overlap"].mean())
merged = d_perf.merge(d_ov_task, on=["mouse", "task"])

fig, axes = plt.subplots(1, len(TASKS),
                         figsize=(len(TASKS) * height, height),
                         sharex=True, sharey=True)
for ax, t in zip(axes, TASKS):
    sub = merged[merged["task"] == t]
    sns.scatterplot(data=sub, x="d_overlap", y="d_perf",
                    hue="mouse", s=100, edgecolor="black",
                    linewidth=0.5, legend=False, ax=ax)
    ax.set_title(t)
    style_axes(ax, "Δ choice overlap", "Δ performance")
    corr_annotate(ax, sub["d_overlap"].values, sub["d_perf"].values)

fig.tight_layout()
fig.savefig(f"{FIG_DIR}/corr_dperf_doverlap_bytask_{dum}.svg")
plt.show()
#+end_src

#+RESULTS:
[[file:./figures/overlaps/figure_43.png]]


#+begin_src jupyter-python
# ------------------------------------------------------------
# Figure 5b: Δ DPA performance (all trials)  |  Δ GNG performance (Dual)
#            vs Δ choice overlap on DPA trials, per mouse
# ------------------------------------------------------------

# --- Δ DPA performance (all trials) ---
sub_dpa = y_single.copy()
sub_dpa["stage"] = normalize_stage(sub_dpa["learning"])
sub_dpa = sub_dpa[(sub_dpa["laser"] == 0)
                  & sub_dpa["stage"].isin(["Naive", "Expert"])]

d_perf_dpa = pivot_delta(
    sub_dpa, index_cols=["mouse"], value_col="performance"
).rename(columns={"delta": "d_perf"})[["mouse", "d_perf"]]

# --- Δ GNG performance (all Dual trials) ---
sub_gng = y_single[y_single["tasks"] != "DPA"].copy()
sub_gng["stage"] = normalize_stage(sub_gng["learning"])
sub_gng = sub_gng[(sub_gng["laser"] == 0)
                  & sub_gng["stage"].isin(["Naive", "Expert"])]

d_perf_gng = pivot_delta(
    sub_gng, index_cols=["mouse"], value_col="odr_perf"
).rename(columns={"delta": "d_perf"})[["mouse", "d_perf"]]

# --- Δ choice overlap (DPA trials only) ---
d_ov_dpa = (
    wide[wide["task"] == "DPA"]
    .groupby("mouse", as_index=False)["d_overlap"].mean()
)

panels = [
    ("DPA (all trials)", d_perf_dpa.merge(d_ov_dpa, on="mouse"),
     "Δ DPA performance"),
    ("GNG (all Dual)",   d_perf_gng.merge(d_ov_dpa, on="mouse"),
     "Δ GNG performance"),
]

fig, axes = plt.subplots(1, 2, figsize=(2 * height, height), sharex=True)

for ax, (title, merged, ylab) in zip(axes, panels):
    sns.scatterplot(data=merged, x="d_overlap", y="d_perf",
                    hue="mouse", s=100, edgecolor="black",
                    linewidth=0.5, legend=False, ax=ax)
    ax.set_title(title)
    style_axes(ax, "Δ choice overlap (DPA)", ylab)
    corr_annotate(ax, merged["d_overlap"].values, merged["d_perf"].values)

fig.tight_layout()
fig.savefig(f"{FIG_DIR}/corr_dperf_dDPAoverlap_DPA_GNG_{dum}.svg")
plt.show()
#+end_src

#+RESULTS:
[[file:./figures/overlaps/figure_44.png]]

#+begin_src jupyter-python

#+end_src

#+RESULTS:

** Read the file in context, draft.org. Read the previous sections in this buffer. Check the results. Propose analysis/figures that align with the draft.

I'll analyze your results in the context of the draft's claims and propose targeted analyses/figures.

- Context recap

Your notebook computes, for each (mouse, learning stage, task/context), *cross-temporal generalization matrices* of a linear choice decoder trained on mPFC population activity, using:
- =mne.GeneralizingEstimator= with logistic regression (CCGD-style)
- Target = =choice= (licks/no-licks)
- Two decoder outputs stacked: calibrated =probas= and raw =decision_function= values ("dfs" = /choice overlap/)
- Correct, non-laser trials, 5-fold stratified CV

Then you extract a scalar /delay-period choice overlap/ per trial (mean over =bins_POST_GNG=, baseline-subtracted) and plot Naive vs Expert per task/context, plus Δperformance vs Δoverlap correlations.

- What the results show (current figures)

From the figures I can read:

1. *Time-courses (Expert vs Naive, per task)*: Choice overlap rises sharply after GNG during delay, strongest for /DualGo/, intermediate /DualNoGo/, weakest /DPA/. Expert mice show *larger, more structured* post-GNG choice signals than Naive, with DualGo showing a clear biphasic lick/no-lick separation.
2. *Naive vs Expert scatters (per mouse, per task)*: Points lie /below identity/ for several mice — i.e., *delay-period choice overlap decreases with learning* on DPA, consistent with the "choice dimension repositioned away from action region" claim.
3. *Δperf vs Δoverlap (per task)*: Negative (but underpowered, p~0.5) correlation for DPA: mice that /reduce/ delay choice overlap tend to /improve/ more.

This is /exactly/ the Fig. 2F–I storyline in the draft: *sample coding preserved, choice/action coding repositioned during the delay, and this repositioning tracks behavioral improvement*.

- Proposed analyses / figures aligned with the draft

*** 1. Directly address Fig. 2F–I: "distance to lick boundary"

Your choice-decoder decision value /is/ (up to sign and norm) a signed distance to the lick boundary. Convert overlap → distance-to-boundary:

#+begin_src jupyter-python :eval no
# distance to boundary = |decision value| / ||w||  (you already divide by ||w||)
# so |overlap| is already a boundary distance in units of 1/||w||
y_single["dist_to_lick"] = np.abs(y_single["choice_overlap_delay"])
#+end_src

Then reproduce draft panels:
- *Fig 2H equivalent*: Δ(delay distance-to-boundary) vs Δ(DPA accuracy) across mice (you already have it — just switch y-axis to distance, not signed overlap).
- *Fig 2I equivalent*: /trial-level/ mixed-effects logistic regression
  #+begin_src python :eval no
  # correct ~ dist_to_lick + task + (1|mouse)
  import statsmodels.formula.api as smf
  m = smf.mixedlm("performance ~ dist_to_lick + C(tasks)",
                  data=df, groups="mouse").fit()
  #+end_src
  This is the single most important missing quantification in your current notebook.

*** 2. Asymmetric Go vs No-Go interference (draft Fig 1D, Fig 2F)

The draft explicitly predicts *Go pushes states toward lick sector, No-Go away*. Test directly on the choice overlap:

- Compute /delay/ (pre-test) choice overlap on =DualGo= vs =DualNoGo= trials (on DPA-correct trials only, conditioned on sample).
- Predict: =overlap_Go > overlap_NoGo=, and the asymmetry ∝ behavioral interference.

Suggested panel: per-mouse paired plot of delay choice overlap (Go – NoGo), colored by ΔDPA accuracy on Go-embedded vs NoGo-embedded trials.

*** 3. Cross-context generalization of the choice axis (draft Fig 2E logic)

Right now you train/test in "all" context. To match the draft's *CCGD* claim you need the cross-context version /for the choice decoder/:

- Train choice decoder on =DPA= delay bins; test on =DualGo= / =DualNoGo= delay.
- Conversely, train on Dual contexts, test on DPA.
- Compare to within-context accuracy. If the /choice axis/ is context-invariant, cross ≈ within.

This is easy given your =dataloader(context="DPA")= already filters; just do the symmetric swap and report a 3×3 context-transfer matrix per mouse, averaged across folds, at the =bins_LD= epoch.

*** 4. Subspace geometry: angle between sample and choice axes (draft Fig 2D right)

You have decoders; extract =w_sample= and =w_choice= per mouse (averaged across CV folds, at the delay epoch). Then:

#+begin_src jupyter-python :eval no
cos_theta = (w_s @ w_c) / (np.linalg.norm(w_s) * np.linalg.norm(w_c))
angle_deg = np.degrees(np.arccos(np.clip(cos_theta, -1, 1)))
#+end_src

Compare to a shuffle null (shuffle neuron identities within one weight vector). Report angle Naive vs Expert — the draft predicts near-orthogonality, increasing with learning.

You already have the =choice= decoder; run the same pipeline for =sample= and =test= and produce a 3×3 pairwise-angle matrix.

*** 5. Single-trial delay-state position (replaces trial-averaged story)

Your per-trial =X_epoch= is already single-trial projections onto the choice axis. Combine with a per-trial sample-axis projection to build the *sample × choice state space* (draft Fig 2F). Then:

- Plot per-trial delay centroids in this 2D plane, colored by outcome.
- Fit logistic boundary, compute distance per trial, feed into mixed-effects model (point 1).
- Compare Naive vs Expert /cloud position/ — the draft's "lower-half-plane shift."

*** 6. Temporal dynamics / attractor reorganization

Use the generalization matrix you already have (=X_single[:,1]=) at the delay band:
- Compute /temporal stability/ = mean off-diagonal / diagonal in =bins_LD= × =bins_LD=.
- Compare Naive vs Expert: higher stability = more attractor-like, consistent with "mnemonic states held farther from boundary" story.

*** 7. Replace/augment the current "per-task scatter"

The current Naive-vs-Expert per-task panel is fine but busy. A cleaner version for the paper:
- x-axis: task (DPA, DualGo, DualNoGo), y-axis: delay choice overlap, hue: stage, lines connecting within-mouse.
- Add paired Wilcoxon per task and linear-mixed-model =overlap ~ stage*task + (1|mouse)=.

- Minimal-effort priority list

If you have limited time, I'd implement in this order:

1. *Trial-level mixed-effects logistic regression* (draft Fig 2I) — 10 lines, high value.
2. *Go vs No-Go delay overlap asymmetry* — directly tests the draft's asymmetric-interference mechanism.
3. *Cross-context CCGD for choice axis* — the =dataloader= is ready; just loop over context pairs.
4. *Sample/choice/test subspace angles* — requires running the pipeline for =sample= and =test= targets; reuses all existing infrastructure.
5. *2D sample–choice state-space plot* — strongest visual match to Fig 2F.

Want me to draft code for any of these? I'd start with (1)+(2) since they directly quantify claims currently only suggested by your scatter plots.

*** provide the codes

Below is a drop-in block that implements the 5 priority analyses. It reuses =X_single=, =y_single=, =X_epoch=, =options=, and your existing =dataloader / ccgd_validation / get_estimator= helpers. Each section is self-contained; run them in order.

- 0. Shared helpers / data prep

#+begin_src jupyter-python
import numpy as np
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns
import statsmodels.formula.api as smf
from scipy.stats import wilcoxon, pearsonr, spearmanr

# ---- Config ----
DELAY_BINS = options["bins_LD"]          # late-delay (pre-test) bins
POSTGNG_BINS = options["bins_POST_GNG"]  # bins you used for X_epoch
TASKS = ["DPA", "DualGo", "DualNoGo"]

# ---- Per-trial scalar: delay choice overlap (abs = distance to lick boundary) ----
# X_epoch is (n_trials, n_times) = signed choice-axis projection, baseline-subtracted
trial_df = y_single.reset_index(drop=True).copy()
trial_df["overlap_delay"] = np.nanmean(X_epoch[:, DELAY_BINS], axis=1)
trial_df["dist_to_lick"]  = np.abs(trial_df["overlap_delay"])
trial_df["stage"] = trial_df["learning"].map(
    lambda s: "Expert" if str(s).lower() in ("1", "expert") else "Naive"
)
trial_df = trial_df[(trial_df.laser == 0) & trial_df.stage.isin(["Naive", "Expert"])]
#+end_src

#+RESULTS:

- 1. Trial-level mixed-effects logistic regression (Fig 2I)

#+begin_src jupyter-python
# correct/error ~ distance-to-lick + task + (1|mouse)
# Only the tasks where "correct" is well-defined for DPA test response:
df_mix = trial_df[trial_df.tasks.isin(TASKS)].copy()
df_mix["correct"] = df_mix["performance"].astype(int)

# Mixed-effects logistic (binomial GLMM via Bayesian-free statsmodels)
# NB: statsmodels has no GLMM logit with random intercepts natively;
# use BinomialBayesMixedGLM for a proper random-intercept logit.
from statsmodels.genmod.bayes_mixed_glm import BinomialBayesMixedGLM

df_mix["mouse_id"] = df_mix["mouse"].astype("category").cat.codes
formula = "correct ~ dist_to_lick + C(tasks) + C(stage)"
random = {"mouse": "0 + C(mouse_id)"}

model = BinomialBayesMixedGLM.from_formula(formula, random, df_mix)
fit = model.fit_vb()
print(fit.summary())

# Effect-size plot: marginal accuracy vs dist_to_lick quintile
df_mix["dist_q"] = pd.qcut(df_mix["dist_to_lick"], 5, labels=False)
agg = (df_mix.groupby(["stage", "dist_q"])["correct"]
              .agg(["mean", "count"]).reset_index())
fig, ax = plt.subplots(figsize=(height, height))
sns.pointplot(data=agg, x="dist_q", y="mean", hue="stage",
              dodge=True, ax=ax, legend=False)
ax.set_xlabel("Distance-to-lick quintile")
ax.set_ylabel("P(correct)")
ax.axhline(0.5, ls="--", color="k")
# plt.savefig(f"{FIG_DIR}/glmm_correct_vs_dist_{dum}.svg")
plt.show()
#+end_src

#+RESULTS:
:RESULTS:
#+begin_example
                    Binomial Mixed GLM Results
===================================================================
                     Type Post. Mean Post. SD   SD  SD (LB) SD (UB)
-------------------------------------------------------------------
Intercept               M     2.7373   0.1303
C(tasks)[T.DualGo]      M    -0.3188   0.2164
C(tasks)[T.DualNoGo]    M    -0.1620   0.2245
C(stage)[T.Naive]       M    -1.5885   0.1370
dist_to_lick            M     0.4006   0.3706
mouse                   V    -0.7486   0.8157 0.473   0.093   2.418
===================================================================
Parameter types are mean structure (M) and variance structure (V)
Variance parameters are modeled as log standard deviations
#+end_example
[[file:./figures/overlaps/figure_51.png]]
:END:

#+begin_src jupyter-python
# Delay choice overlap on DPA-correct trials, Go vs NoGo embedded
d = trial_df[(trial_df.performance == 1) & (trial_df.tasks != "DPA")].copy()
g = (d.groupby(["mouse", "stage", "tasks"])["overlap_delay"]
       .mean().unstack("tasks").reset_index())
g["Go_minus_NoGo"] = g["DualGo"] - g["DualNoGo"]

# Paired Wilcoxon per stage
for st, sub in g.groupby("stage"):
    w = wilcoxon(sub["DualGo"], sub["DualNoGo"])
    print(f"{st}: Go={sub['DualGo'].mean():.3f}  NoGo={sub['DualNoGo'].mean():.3f}"
          f"  W={w.statistic:.1f}  p={w.pvalue:.3g}  N={len(sub)}")

# Paired plot
fig, axes = plt.subplots(1, 2, figsize=(2*height, height), sharey=True)
for ax, st in zip(axes, ["Naive", "Expert"]):
    sub = g[g.stage == st]
    for _, r in sub.iterrows():
        ax.plot([0, 1], [r["DualNoGo"], r["DualGo"]], "-o",
                color="0.3", alpha=0.7)
    ax.set_xticks([0, 1]); ax.set_xticklabels(["NoGo", "Go"])
    ax.set_title(st); ax.set_ylabel("Delay choice overlap")
    ax.axhline(0, ls="--", color="k")
plt.tight_layout()
# plt.savefig(f"{FIG_DIR}/go_vs_nogo_delay_overlap_{dum}.svg")
plt.show()
#+end_src

#+RESULTS:
:RESULTS:
: Expert: Go=-0.173  NoGo=-0.349  W=0.0  p=1  N=1
: Naive: Go=-0.043  NoGo=0.017  W=0.0  p=1  N=1
[[file:./figures/overlaps/figure_52.png]]
:END:

#+begin_src jupyter-python
# Correlate interference asymmetry with behavioral asymmetry
perf = (y_single[y_single.tasks != "DPA"]
        .assign(stage=lambda d: d.learning.map(
            lambda s: "Expert" if str(s).lower() in ("1","expert") else "Naive"))
        .groupby(["mouse","stage","tasks"])["performance"].mean()
        .unstack("tasks").reset_index())
perf["dPerf"] = perf["DualGo"] - perf["DualNoGo"]
m = g.merge(perf[["mouse","stage","dPerf"]], on=["mouse","stage"])

fig, ax = plt.subplots(figsize=(height, height))
sns.scatterplot(data=m, x="Go_minus_NoGo", y="dPerf",
                hue="stage", style="mouse", s=90, ax=ax, legend=False)
corr_annotate(ax, m["Go_minus_NoGo"].values, m["dPerf"].values)
style_axes(ax, "Δ overlap (Go–NoGo)", "Δ DPA perf (Go–NoGo)")
# plt.savefig(f"{FIG_DIR}/asymmetry_overlap_vs_perf_{dum}.svg")
plt.show()
#+end_src

#+RESULTS:
[[file:./figures/overlaps/figure_53.png]]

- 3. Cross-context CCGD for the choice axis

You already compute generalization over /time/ via =GeneralizingEstimator=. For cross-/context/, re-run =ccgd_validation= with single-context =X_within= and let =X_cross= carry the other contexts.

#+begin_src jupyter-python
# Cross-context decoding of CHOICE: train in one context, test on others.
# Uses your existing dataloader + ccgd_validation.
from itertools import product

contexts = ["DPA", "DualGo", "DualNoGo"]
n_splits, n_repeats = 5, 1
folds = RepeatedStratifiedKFold(n_splits=n_splits, n_repeats=n_repeats)
estimator = get_estimator(clf="logcv", scoring="accuracy",
                          mode="sliding", scaler=scaler,
                          l1_ratio=l1_ratio, n_jobs=-1)

rows = []
for mouse in options["mice"]:
    for stage in ["Naive", "Expert"]:
        idx = (y_all.mouse == mouse) & (y_all.learning == stage) & (y_all.laser == 0)
        X_m = X_all[idx]
        y_m = y_all.loc[idx].reset_index(drop=True)
        valid = ~np.all(np.isnan(X_m), axis=(0, 2))
        X_m = X_m[:, valid, :]
        X_m = gaussian_filter1d(X_m, sigma=0.3, axis=-1)

        for train_ctx in contexts:
            m_tr = (y_m.tasks == train_ctx)
            if m_tr.sum() < 20: continue
            X_tr_ctx = X_m[m_tr]
            y_tr_ctx = y_m.loc[m_tr].reset_index(drop=True).copy()
            y_tr_ctx["labels"] = y_tr_ctx["choice"].values

            for test_ctx in contexts:
                m_te = (y_m.tasks == test_ctx)
                if m_te.sum() < 20: continue
                X_te_ctx = X_m[m_te]
                y_te_ctx = y_m.loc[m_te].reset_index(drop=True).copy()
                y_te_ctx["labels"] = y_te_ctx["choice"].values

                strata = y_tr_ctx["labels"].astype(str).values
                if train_ctx == test_ctx:
                    probas, dfs, y_cv, _ = ccgd_validation(
                        (X_tr_ctx, y_tr_ctx,
                         X_tr_ctx[:0], y_tr_ctx.iloc[:0], strata),
                        estimator, cv=folds, raw=False, signed=False)
                    score = np.mean(probas[:, DELAY_BINS] > 0.5)
                else:
                    probas, dfs, y_cv, _ = ccgd_validation(
                        (X_tr_ctx, y_tr_ctx,
                         X_te_ctx, y_te_ctx, strata),
                        estimator, cv=folds, raw=False, signed=False)
                    # take cross-context trials (after within in y_cv)
                    is_cross = y_cv.index >= len(y_tr_ctx)
                    score = np.mean(probas[is_cross][:, DELAY_BINS] > 0.5)

                rows.append(dict(mouse=mouse, stage=stage,
                                 train=train_ctx, test=test_ctx,
                                 acc=score))

ccgd = pd.DataFrame(rows)
pkl_save(ccgd, f"ccgd_choice_crosscontext_{dum}", path="../data/overlaps/")

# Plot 3x3 transfer matrix per stage (averaged across mice)
fig, axes = plt.subplots(1, 2, figsize=(2*height, height), sharey=True)
for ax, st in zip(axes, ["Naive", "Expert"]):
    M = (ccgd[ccgd.stage == st]
         .groupby(["train", "test"])["acc"].mean()
         .unstack("test").reindex(index=contexts, columns=contexts))
    sns.heatmap(M, annot=True, fmt=".2f", cmap="viridis",
                vmin=0.5, vmax=1.0, ax=ax,
                cbar_kws={"label": "delay choice acc"})
    ax.set_title(st); ax.set_xlabel("test ctx"); ax.set_ylabel("train ctx")
plt.tight_layout()
#plt.savefig(f"{FIG_DIR}/ccgd_choice_ctx_{dum}.svg")
plt.show()
#+end_src

#+RESULTS:


- 4. Subspace angles between sample / choice / test axes (Fig 2D right)

#+begin_src jupyter-python
def fit_axis(X, y_df, target, bins, stage, context="all",
             estimator=None, folds=None, n_avg=True):
    """Return a normalized decoder weight vector for target at given epoch."""
    mask = ((y_df.learning == stage) & (y_df.laser == 0))
    if context != "all":
        mask &= (y_df.tasks == context)
    Xm = X[mask]; ym = y_df.loc[mask].reset_index(drop=True).copy()
    ym["labels"] = ym[target].values

    # Average X over epoch -> (n_trials, n_features)
    Xe = np.nanmean(Xm[..., bins], axis=-1)
    est = clone(estimator)
    est.fit(Xe, ym["labels"].values)
    # inside SlidingEstimator? No — we used mode=None earlier; strip Pipeline
    if hasattr(est, "named_steps"):
        w = est.named_steps["model"].coef_.ravel()
    else:
        w = est.coef_.ravel()
    w = w / (np.linalg.norm(w) + 1e-12)
    return w

def subspace_angle(w1, w2):
    c = np.clip(np.dot(w1, w2), -1, 1)
    # principal angle on 1-D subspaces = acos(|cos|)
    return np.degrees(np.arccos(abs(c)))

# Static (non-temporal) pipeline for weight extraction
clf_static = get_estimator(clf="logcv", scoring="accuracy",
                           mode=None, scaler=scaler,
                           l1_ratio=l1_ratio, n_jobs=-1)

targets = {
    "sample": options["bins_ED"],    # early delay, sample-dominant
    "choice": options["bins_LD"],    # late delay, choice-dominant
    "test":   options["bins_TEST"],  # test window
}

angle_rows = []
for mouse in options["mice"]:
    idx = (y_all.mouse == mouse) & (y_all.laser == 0)
    X_m = X_all[idx]; y_m = y_all.loc[idx].reset_index(drop=True)
    valid = ~np.all(np.isnan(X_m), axis=(0, 2))
    X_m = X_m[:, valid, :]

    for stage in ["Naive", "Expert"]:
        try:
            ws = {t: fit_axis(X_m, y_m, t, b, stage,
                              estimator=clf_static) for t, b in targets.items()}
        except Exception as e:
            print(mouse, stage, e); continue
        for a, b in [("sample","choice"),("sample","test"),("choice","test")]:
            # shuffle null
            rng = np.random.default_rng(0)
            null = np.array([subspace_angle(ws[a],
                                            rng.permutation(ws[b]))
                             for _ in range(200)])
            obs = subspace_angle(ws[a], ws[b])
            angle_rows.append(dict(mouse=mouse, stage=stage,
                                   pair=f"{a}-{b}", angle=obs,
                                   null_mean=null.mean(),
                                   p=np.mean(null >= obs)))
angles = pd.DataFrame(angle_rows)
#+end_src

#+begin_src jupyter-python
# Plot
fig, ax = plt.subplots(figsize=(1.6*height, height))
sns.boxplot(data=angles, x="pair", y="angle", hue="stage", ax=ax)
sns.stripplot(data=angles, x="pair", y="angle", hue="stage",
              dodge=True, ax=ax, color="k", size=4, legend=False)
ax.axhline(90, ls="--", color="r", label="orthogonal")
# ax.set_ylim(0, 95); ax.set_ylabel("Subspace angle (deg)")
plt.tight_layout()
plt.savefig(f"{FIG_DIR}/subspace_angles_{dum}.svg")
plt.show()
#+end_src

#+RESULTS:
[[file:./figures/overlaps/figure_56.png]]

- 5. 2D sample × choice state space with speed map (Fig 2F–G)

#+begin_src jupyter-python
from scipy.stats import gaussian_kde

def project_2d(X, y_df, bins_epoch, stage, estimator):
    """Return per-trial (sample_proj, choice_proj) averaged over bins_epoch,
       and trial metadata."""
    mask = (y_df.learning == stage) & (y_df.laser == 0)
    Xm = X[mask]; ym = y_df.loc[mask].reset_index(drop=True)
    valid = ~np.all(np.isnan(Xm), axis=(0, 2))
    Xm = Xm[:, valid, :]
    Xe = np.nanmean(Xm[..., bins_epoch], axis=-1)

    # Fit sample and choice axes on same epoch (or use pre-fit axes)
    w_s = clone(estimator).fit(Xe, ym["sample"].values)
    w_c = clone(estimator).fit(Xe, ym["choice"].values)
    ws = w_s.named_steps["model"].coef_.ravel()
    wc = w_c.named_steps["model"].coef_.ravel()
    ws /= np.linalg.norm(ws) + 1e-12
    wc /= np.linalg.norm(wc) + 1e-12

    proj_s = Xe @ ws
    proj_c = Xe @ wc
    return proj_s, proj_c, ym

fig, axes = plt.subplots(1, 2, figsize=(2*height, height),
                         sharex=True, sharey=True)
for ax, stage in zip(axes, ["Naive", "Expert"]):
    S_all, C_all = [], []
    for mouse in options["mice"]:
        idx = y_all.mouse == mouse
        try:
            s, c, ym = project_2d(X_all[idx], y_all.loc[idx].reset_index(drop=True),
                                  DELAY_BINS, stage, clf_static)
        except Exception:
            continue
        S_all.append(s); C_all.append(c)
    S = np.concatenate(S_all); C = np.concatenate(C_all)

    # Speed map proxy: inverse density (slow regions ≈ high density)
    xy = np.vstack([S, C])
    kde = gaussian_kde(xy, bw_method=0.25)
    xs = np.linspace(S.min(), S.max(), 80)
    ys = np.linspace(C.min(), C.max(), 80)
    XX, YY = np.meshgrid(xs, ys)
    ZZ = kde(np.vstack([XX.ravel(), YY.ravel()])).reshape(XX.shape)

    ax.contourf(XX, YY, -ZZ, levels=20, cmap="magma", alpha=0.7)
    ax.scatter(S, C, s=4, c="w", alpha=0.4, edgecolor="none")
    ax.axhline(0, ls="--", color="w"); ax.axvline(0, ls="--", color="w")
    ax.set_xlabel("sample axis"); ax.set_ylabel("choice axis")
    ax.set_title(stage)
plt.tight_layout()
plt.savefig(f"{FIG_DIR}/state_space_sample_choice_{dum}.svg")
plt.show()
#+end_src

#+RESULTS:
[[file:./figures/overlaps/figure_57.png]]

- Notes / caveats

- In Section 1, if =BinomialBayesMixedGLM= is too slow, fall back to a clustered logit: =smf.logit("correct ~ dist_to_lick + C(tasks) + C(stage)", data=df_mix).fit(cov_type="cluster", cov_kwds={"groups": df_mix["mouse"]})=.
- Section 3 assumes balanced classes per context; if a mouse has too few trials for a label in some context, skip that (train, test) cell.
- Section 4 fits axes on /epoch-averaged/ activity to get a single weight vector; for a stricter reading of the draft's dPCA subspaces, replace =fit_axis= with top-/k/ dPCA components per marginal.
- Section 5 uses /density/ as an inverse speed proxy — fine for a first pass; to literally match "local trajectory speed" compute =||x(t+1)-x(t)||= per trial and bin over the (sample, choice) plane.
- =corr_annotate=, =style_axes=, =FIG_DIR=, =dum=, =height=, =width= are those defined earlier in your notebook.

Want me to also add (a) a Δ(delay distance-to-boundary) vs Δ(DPA accuracy) panel (draft Fig 2H), and (b) a within-session stability control (first-half vs second-half delay centroid), matching Supp. Fig 4d?

***
